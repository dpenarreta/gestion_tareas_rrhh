# Decisiones de Arquitectura de Nexo

> Bitácora liviana de decisiones de arquitectura y diseño técnico — un
> índice escaneable de "qué se decidió y por qué", en una línea o un párrafo
> corto por decisión. Para el análisis completo (problema/alternativas/
> justificación/impacto) de las decisiones más significativas, ver
> `docs/AUDIT_LOG.md`, enlazado desde cada fila cuando exista.

| Decisión | Motivo técnico | Detalle completo |
|---|---|---|
| `unsafe-eval` en la CSP condicionado a `!isProd` en vez de quitarlo del todo (2026-09-01) | React lo necesita solo en desarrollo (mensaje propio de React en consola); producción compila y corre sin él, verificado con `npm run build` | `AUDIT_LOG.md` § 2026-09-01 ("Corrección de los hallazgos de la auditoría de seguridad") |
| No forzar `npm audit fix --force` para protobufjs/sharp vía @xenova/transformers (2026-09-01) | Degradaría `@xenova/transformers` a 1.4.2 (breaking, motor de embeddings de Nova) sin evaluar el impacto — la vulnerabilidad es real pero de menor severidad práctica que romper esa feature sin aviso | `AUDIT_LOG.md` § 2026-09-01 ("Corrección de los hallazgos de la auditoría de seguridad") |
| Validación de `assigned_to` en `TaskService` (services.py), no en los serializers (2026-09-01) | Necesita al actor autenticado, no solo el dato del campo — mismo criterio ya usado por `update_task`/`SELF_ONLY_FIELDS`: la autorización de negocio vive en el service | `AUDIT_LOG.md` § 2026-09-01 ("Corrección de los hallazgos de la auditoría de seguridad") |
| "SuperUsuario" = ADMINISTRADOR con TODO el catálogo sembrado explícito en la base (2026-09-01), no un rol nuevo | El usuario pidió "un rol de SuperUsuario"; `AskUserQuestion` reveló que ADMINISTRADOR ya cumple esa función (bypass `is_superuser`) y que un 12° rol rompería el union type fijo de 11 valores de `Role` sin necesidad real | `AUDIT_LOG.md` § 2026-09-01 ("SuperUsuario = ADMINISTRADOR con todo el catálogo explícito") |
| La migración 0004 le siembra `escritorio_digital.usar` a ADMINISTRADOR en el backend, pese a la exclusión de Escritorio Digital documentada más abajo ("Escritorio Digital excluye al Administrador") | Pedido explícito y literal del usuario ("todo por defecto", sin excepciones) — solo afecta al caso ADMINISTRADOR-por-grupo-sin-superusuario (un ADMINISTRADOR real ya tenía acceso vía `is_superuser`). `src/lib/roles.ts::canUseDeskNotes` NO se tocó — el link de navegación sigue oculto para `role === "ADMINISTRADOR"` en el frontend, así que la exclusión de UX se mantiene aunque el backend ahora lo permitiría | `AUDIT_LOG.md` § 2026-09-01 ("SuperUsuario = ADMINISTRADOR con todo el catálogo explícito") |
| Catálogo dinámico de permisos extendido a todo el sistema, 19 módulos (2026-09-01), en vez de limitarse al catálogo administrativo existente | Elección explícita del usuario ante el trade-off presentado — la pantalla nueva debía controlar la autorización real, no solo una fracción administrativa cosmética | `AUDIT_LOG.md` § 2026-09-01 ("Catálogo dinámico de permisos extendido a todo el sistema") |
| `/admin/roles` no crea/elimina roles pese a que `RoleViewSet` (backend) ya soporta POST/DELETE completo | `Role` es un union type TS fijo de 11 valores consumido sin `default`/fallback por ~15 funciones de `roles.ts` — un `Group` nuevo creado desde el backend rompería en runtime en el resto del frontend; el proxy Next.js solo reenvía GET/PATCH | `AUDIT_LOG.md` § 2026-09-01 ("Catálogo dinámico de permisos extendido a todo el sistema") |
| Migración de datos del catálogo nuevo siembra explícitamente `ADMINISTRADOR` en 9 de 10 módulos, en vez de confiar solo en el bypass de `is_superuser` | `role_name()`/`role_level()` (legacy) tratan la pertenencia al grupo `ADMINISTRADOR` como equivalente a superusuario; `user_has_permission` (catálogo dinámico) no replica esa equivalencia — un ADMINISTRADOR-solo-por-grupo (`UserAdminService.create_user` nunca asigna `is_superuser`) es un estado real alcanzable, confirmado por 23 fallas de test en la primera versión de la migración | `AUDIT_LOG.md` § 2026-09-01 ("Catálogo dinámico de permisos extendido a todo el sistema") |
| Retiro completo de `legacy_postgres_id` y del puente cuid↔id-Django (2026-08-31) — cierra el punto pendiente de la decisión anterior | El usuario reconfirmó explícitamente el retiro tras ver el resumen del decommission de Postgres; el campo ya no conectaba con nada externo (Prisma retirado desde la Fase 90) y causaba 2 bugs activos (guard de autoeliminación inerte, `viewPreferences` con 404 interno) | `AUDIT_LOG.md` § 2026-08-31 ("Retiro completo de legacy_postgres_id y del puente de id cuid↔Django") |
| Decommission de la conexión al Postgres legacy (2026-08-31): se elimina la infraestructura de importación (41 comandos + helper + `LEGACY_POSTGRES_URL`), pero se conserva `legacy_postgres_id` (el campo) | Los comandos nunca se ejecutaron y no se van a ejecutar (nueva base SQL Server en otro servidor); `legacy_postgres_id` en cambio estaba activo hasta esa fecha (`RosterView`, Reportes Ejecutivos) — se retiró aparte en la decisión siguiente, ver fila anterior | `AUDIT_LOG.md` § 2026-08-31 ("Decommission de la conexión al Postgres legacy") |
| Reemplazo de Groq por Google Gemini como proveedor de IA de Nova (2026-08-31), sin capa de abstracción multi-proveedor | Pedido explícito del usuario, reemplazo no configuración; `@google/genai` (SDK activo) en vez de `@google/generative-ai` (legado, congelado); `gemini-3.6-flash` en vez de `gemini-2.5-flash` (este último ya no disponible para cuentas nuevas, verificado contra la API real) | `AUDIT_LOG.md` § 2026-08-31 ("Reemplazo del proveedor de IA de Nova: Groq → Google Gemini") |
| `gemini-3.6-flash`: se omite `thinkingConfig` en vez de fijar `thinkingBudget: 0` | Este modelo rechaza `thinkingBudget: 0` con 400 (a diferencia de la familia 2.5) y no respeta de forma confiable un budget bajo explícito — se compensa subiendo `maxOutputTokens` con margen amplio en los 6 call sites, verificado en vivo contra la API real. | `AUDIT_LOG.md` § 2026-08-31 ("Reemplazo del proveedor de IA de Nova: Groq → Google Gemini") |
| Timeout dedicado `ANALYTICS_BUNDLE_TIMEOUT_MS` (12s) solo para los consumidores del bundle de Analytics (2026-08-31), en vez de subir el `REQUEST_TIMEOUT_MS` global (3s) | `GET /analytics/<id>/` no tiene caché con TTL y toma ~2.8s de cómputo puro — subir el timeout global degradaría el fail-fast deseado para login/CRUD liviano en toda la app. | `AUDIT_LOG.md` § 2026-08-31 ("Timeout dedicado para el bundle de Analytics") |
| Decommission del servicio físico de Postgres (2026-08-28): se detiene y se pone en `StartType: Manual`, NO se desinstala ni se borra el directorio de datos | Acción reversible por decisión explícita del usuario — posible presencia de datos reales de personal, desinstalar/borrar es irreversible y no se necesitaba para el objetivo (dejar de correr en segundo plano). | `AUDIT_LOG.md` § 2026-08-28 ("Decommission del servicio físico de PostgreSQL local") |
| Re-medición de performance (2026-08-28): se mide contra `manage.py runserver`, no gunicorn real | Docker no pudo descargar la imagen base en este sandbox (bloqueo de TLS/red al registry, sin imagen cacheada ni WSL utilizable) — gunicorn tampoco corre nativo en Windows. Confirmado con el usuario antes de seguir: medición aproximada en vez de detener el trabajo. | `AUDIT_LOG.md` § 2026-08-28 ("Re-medición de performance") |
| Re-medición de performance: se usa el `next dev` ya activo del usuario en vez de copiar el repo a una carpeta temporal | Next.js bloquea un segundo `next dev` en el mismo directorio (lock por proyecto, no por puerto) — copiar el repo era más lento y el usuario prefirió reusar su servidor ya corriendo. | `AUDIT_LOG.md` § 2026-08-28 ("Re-medición de performance") |
| Cierre de `notification_rules`: `.exclude(id=author.id)` se agrega en `CommentService.create_comment` pero NO en `ActivityService.create_retroactive_activity` | Comentarios: la config ahora libremente editable introduce un riesgo de auto-notificación que la jerarquía fija anterior nunca tuvo — salvaguarda nueva y justificada. Retroactivo: el actor YA no se excluía en el comportamiento actual (verificado en código) — preservar tal cual, no corregir una asimetría preexistente no pedida. | `AUDIT_LOG.md` § 2026-08-28 ("Cierre del gap notification_rules") |
| Cierre de `notification_rules`: `notify_if_high_risk` (`apps/analytics/services.py`) queda fuera de alcance pese a compartir el mismo patrón hardcodeado | Es un consumidor distinto (riesgo alto, no comentarios) sin campo propio en la config — reusar `comment_targets` ahí sería semánticamente incorrecto; cerrarlo necesita un campo nuevo, decisión de producto aparte. | `AUDIT_LOG.md` § 2026-08-28 ("Cierre del gap notification_rules") |
| Fases 87-90: el decommission de Prisma/PostgreSQL se limita a código/repo — NO se toca el servicio de Windows `postgresql-x64-16` ni el hosting Neon | Esa infraestructura probablemente tiene datos reales de personal (`docs/ARCHITECTURE.md` documenta que no hay BD de desarrollo separada) — decisión confirmada explícitamente con el usuario vía `AskUserQuestion` antes de planificar, por implicancias legales/LOPDP que exceden una tarea de código. | `AUDIT_LOG.md` § 2026-08-28 (Fases 87-90) |
| Fase 90: los 79 imports de tipo puro desde el cliente Prisma se redefinen en el módulo TS que ya era dueño de ese dominio, no en un archivo de tipos centralizado nuevo | `Role` (74 de 79 imports) ya tenía su hub natural en `src/lib/roles.ts` (`ALL_ROLES`/`ROLE_LABEL`); crear un archivo de enums nuevo habría sido una capa de indirección innecesaria para tipos que ya vivían de facto en un módulo de dominio. | `AUDIT_LOG.md` § 2026-08-28 (Fases 87-90) |
| Fase 88: `AnalyticsDiagnosticsView` se crea como vista Django dedicada en vez de extender `DataQualityView` (`scope=self\|team`) con un tercer `scope=all` | Mezclar el concepto "calidad de datos" con el versionado del motor (`recordEngineVersionIfChanged`) en la misma vista de scope-based de `DataQualityView` habría sido menos claro que un endpoint dedicado al panel admin que realmente los necesita juntos. | `AUDIT_LOG.md` § 2026-08-28 (Fases 87-90) |
| Fase 86: se elimina `projectAccess.ts` en vez de adaptarlo a la forma anidada de Django | Comparaba el `cuid` de Postgres de `session.userId` contra ids que, con la página cortada a Django, pasan a ser el id numérico — nunca hubiera coincidido. Django ya aplica la misma fórmula exacta server-side (`CanAccessProject`/`CanManageProject`/`CanDeleteProject`), verificado línea por línea; solo `canManage`/`canDelete` (UI) se recalculan en TS. | `AUDIT_LOG.md` § 2026-08-28 (Fase 86) |
| Fase 86: `resolveRoster.ts` queda fuera de alcance del cutover | Lógica de negocio propia de Reportes Ejecutivos (exclusión de roles de liderazgo, narrowing por filtros, `scope`/`rosterKind`) sin equivalente en ningún endpoint Django genérico de usuarios — portarla es una fase aparte con diseño propio. | `AUDIT_LOG.md` § 2026-08-28 (Fase 86) |
| Fase 85: reusar `/predictive/predictions/<id>/`/`/kpis/team-capacity/` en vez de construir 3 endpoints Django nuevos de datos crudos (permisos/estados especiales/auditoría) | La investigación previa (3 agentes Explore) confirmó que `predictionEngine.ts`/`capacityForecast.ts`/`trendEngine.ts` ya eran réplicas exactas de código Django ya vivo desde la Fase 48 — la hipótesis original de "hace falta portar" era incorrecta. | `AUDIT_LOG.md` § 2026-08-27 (Fase 85) |
| Fase 85: nueva vista `TeamSubutilizationReportView` (roster explícito) en vez de extender la `TeamSubutilizationView` existente (equipo jerárquico del actor) | Son semánticas distintas — mezclar ambas en una sola vista habría acoplado el resultado del reporte a quién lo genera, rompiendo la inmutabilidad/reproducibilidad del snapshot. | `AUDIT_LOG.md` § 2026-08-27 (Fase 85) |
| Fase 84: los 4 endpoints Django reconectados en `systemConfig.ts` solo exponen el valor efectivo AHORA, no vigente en una fecha pasada (a diferencia de la función Prisma original) | Mismo trade-off ya aceptado en la Fase 73 para `workday_end_hour` — evita construir soporte `?as_of=` nuevo en 4 vistas Django para un caso de uso (reportes históricos con config ya cambiada) de bajo impacto práctico. | `AUDIT_LOG.md` § 2026-08-27 (Fase 84) |
| Fase 84: `analytics-config`/`normalization-curves`/`prediction-window` route.ts se simplifican para confiar en la validación que Django ya hace, en vez de duplicarla | Mismo patrón ya usado en `workload-config/route.ts` (Fase 52) — Django ya valida las 3 sumas de ponderación + orden de umbrales, verificado línea por línea contra el TS original. | `AUDIT_LOG.md` § 2026-08-27 (Fase 84) |
| Fase 83: la limpieza de GitHub de documentos purgados se hace DESPUÉS de borrar en la base (Django), no antes como en el TS original | Ya era best-effort/no bloqueante en ambos extremos (`.catch()`) — invertir el orden da el mismo resultado final y evita una segunda ida y vuelta HTTP entre `route.ts` y Django. | `AUDIT_LOG.md` § 2026-08-27 (Fase 83) |
| Fase 83: `MonthlyReport`/`DataPurgeLog` se portan sin `legacy_postgres_id` ni comando de importación, a diferencia de los 42 modelos ya portados | Decisión explícita del usuario (Fase 82): nunca migrar datos históricos reales — confirmada de nuevo puntualmente para estos 2 modelos antes de implementar. | `AUDIT_LOG.md` § 2026-08-27 (Fase 83) |
| Fase 82: la ejecución real de los 40 comandos de migración contra Postgres de producción queda descartada (no diferida) — se sigue probando exclusivamente con datos sintéticos | El usuario aclaró explícitamente que no quiere recuperar ningún dato histórico real; deja de ser un bloqueante pendiente en `docs/VERSION.md`/`docs/ROADMAP.md` para pasar a alcance fuera de esta migración. | `AUDIT_LOG.md` § 2026-08-27 (Fase 82) |
| Fase 81: `MonthlyReport`/`DataPurgeLog` quedan fuera de las Waves 1-4 — es un port de FEATURE completo (uso activo real en `retentionPolicy.ts`/`buildSnapshotData.ts`), no una migración de datos | Investigar antes de escribir código reveló que no son datos históricos congelados: mezclar ambos alcances en la misma fase habría requerido diseñar la lógica de purga en Python sin que el usuario lo pidiera explícitamente. | `AUDIT_LOG.md` § 2026-08-27 (Fase 81) |
| Fase 81: la ejecución real de los 40 comandos contra Postgres de producción queda documentada como pendiente, sin bloquear el resto del trabajo | El usuario eligió explícitamente seguir con todo lo ejecutable (código + verificación sintética) en vez de detener la sesión a esperar credenciales reales. | `AUDIT_LOG.md` § 2026-08-27 (Fase 81) |
| Fase 81: `bulk_import_rows` solo corrige un campo `auto_now` si el snapshot capturó un valor no-`None` | Un modelo sin timestamp legacy real (`MeetingInvitee`) reveló que corregir con `None` pisaba el valor "ahora" correcto que `bulk_create` ya había generado — bug real, reproducido con datos sintéticos antes de llegar a producción. | `AUDIT_LOG.md` § 2026-08-27 (Fase 81) |
| Fase 80: se construye el cimiento reutilizable (esquema + módulo compartido) y se prueba con Wave 0 (2 comandos) antes de escribir las ~40 entidades de una sola pasada | Mismo criterio que ya evitó bugs reales en esta migración (Fase 52, Fase 70): probar el patrón a escala pequeña antes de replicarlo 38 veces más — de hecho reveló un bug real (`bulk_create` sí dispara `auto_now`) que habría corrompido timestamps en las 40 entidades si se hubiera asumido sin probar. | `AUDIT_LOG.md` § 2026-08-27 (Fase 80) |
| Fase 80: idempotencia de los comandos de migración vía `filter_not_yet_imported` (prefiltrar antes de insertar), no vía `bulk_create(ignore_conflicts=True)` | `mssql-django` (el driver de producción) declara `supports_ignore_conflicts = False` — el mecanismo estándar de Postgres no está disponible contra SQL Server. | `AUDIT_LOG.md` § 2026-08-27 (Fase 80) |
| Fase 80: `bulk_import_rows` hace `bulk_create()` + `bulk_update()` de corrección para `created_at`/`updated_at`, en vez de asumir que `bulk_create` preserva esos valores | Verificado empíricamente que `bulk_create` SÍ dispara `pre_save()`/`auto_now` igual que `.save()` (contradice la asunción inicial) — `.update()`/`bulk_update()` es el único camino de escritura que no pasa por `pre_save()`. | `AUDIT_LOG.md` § 2026-08-27 (Fase 80) |
| 2026-08-27: se corrige `docs/VERSION.md`/`docs/ROADMAP.md` para distinguir "cutover de ruta" (código, 100% completo) de "migración de datos reales" (pendiente en su totalidad salvo Usuarios) | Las 2 cosas se venían tratando como equivalentes — un corte real a producción basado en la lectura de esos documentos habría asumido, incorrectamente, que solo faltaba correr `migrate_users_from_postgres`. | `AUDIT_LOG.md` § 2026-08-27 (hallazgo de migración de datos) |
| 2026-08-27: no se emprende la escritura de los ~40 comandos de migración de datos en la misma respuesta que el hallazgo | Es un trabajo sustancial (~40 scripts + 2 decisiones de producto sobre modelos sin puerto) que amerita su propio alcance, a definir explícitamente con el usuario en vez de asumir. | `AUDIT_LOG.md` § 2026-08-27 (hallazgo de migración de datos) |
| Fase 79: "Dashboard/Analytics" del FPS §15 se interpreta como `GET /kpis/team/`+`GET /analytics/<id>/`, no como `GET /api/dashboard` literal | `/api/dashboard` no tiene ningún KPI para comparar (solo tareas/comunicados/reuniones/proyectos) — las pantallas de KPIs/Analytics sí tienen los mismos números que el snapshot. | `AUDIT_LOG.md` § 2026-08-27 (Fase 79) |
| Fase 79: la validación de integridad solo corre para reportes MENSUAL del mes en curso sin `fechaCorte` explícita | `TeamKpiView` (la fuente de comparación) no es consciente de la fecha de corte de un reporte — compararla contra reportes históricos/con corte generaría discrepancias falsas por diseño, no bugs reales. | `AUDIT_LOG.md` § 2026-08-27 (Fase 79) |
| Fase 79: se crea un modelo Django nuevo (`ExecutiveReportIntegrityIncident`) en vez de extender `ExecutiveReportAuditLog` | `ExecutiveReportAuditLog` es un log append-only de "quién/cuándo/qué pasó", sin campos estructurados para expected/actual — el FPS pide algo trackeable como incidente, no una línea de auditoría más. | `AUDIT_LOG.md` § 2026-08-27 (Fase 79) |
| Fase 79: no se compara el Índice Ejecutivo (`fetchPerformanceAndHealth`) contra sí mismo | Sería casi tautológico — ya usa el mismo endpoint (`/analytics/<id>/`) que el builder llama para calcularlo; `TeamKpiView` sí es un camino de cómputo Python genuinamente distinto, la comparación que realmente vale. | `AUDIT_LOG.md` § 2026-08-27 (Fase 79) |
| Fase 78: se sube `--threads` de gunicorn en vez de `--workers` | La carga que causa el hallazgo de la Fase 77 es de I/O (espera de red hacia SQL Server), no de CPU — un worker `gthread` atiende varios requests I/O-bound concurrentes dentro del mismo proceso, mucho más barato en memoria que clonar el proceso completo por cada unidad de concurrencia adicional. | `AUDIT_LOG.md` § 2026-08-27 (Fase 78) |
| Fase 78: se aplica el fix de gunicorn sin re-verificar contra un gunicorn real primero | El usuario eligió explícitamente no bloquear en reconstruir el entorno sintético una vez más — la aritmética de cola (workers × tiempo por request) explica el hallazgo de la Fase 77 con precisión suficiente para justificar el cambio sin una nueva medición. | `AUDIT_LOG.md` § 2026-08-27 (Fase 78) |
| Fase 77: se reconstruye el escenario sintético completo (Postgres + Django) para remedir Sprint Q, en vez de una medición parcial solo del lado Django | El problema cambió de forma con la migración (2 de 4 funciones lentas pasaron a HTTP) — una medición parcial no habría dado un número directamente comparable al ~22s original. Elegido explícitamente por el usuario. | `AUDIT_LOG.md` § 2026-08-26 (Fase 77) |
| Fase 77: `scripts/bench-executive-report.ts` se reescribe para usar login real + HTTP real contra el endpoint de producción, en vez de parchear `djangoSession.ts`/`cookies()` para que el script viejo vuelva a funcionar | Tocar código de sesión compartido solo para que un script de benchmark funcione es más riesgoso que medir vía el camino real que ya sigue cualquier usuario — y de paso mide con más fidelidad (incluye la capa HTTP real). | `AUDIT_LOG.md` § 2026-08-26 (Fase 77) |
| Fase 77: se usa el comando real `migrate_users_from_postgres` (apuntado temporalmente a la BD sintética) en vez de escribir un comando de management temporal para sembrar Django | Reutiliza código ya probado en vez de escribir código nuevo desechable — y valida de paso ese comando, bloqueado en el resto de la migración por falta de credenciales reales. | `AUDIT_LOG.md` § 2026-08-26 (Fase 77) |
| Fase 77: no se investiga la causa exacta de la degradación bajo concurrencia (contención de conexiones SQL Server, límites de `runserver`, etc.) ni se implementa ninguna corrección en esta misma fase | El alcance acordado con el usuario fue "solo medir" — la causa raíz exacta y la corrección quedan para una fase de optimización separada, ya con datos reales para decidir el alcance correcto. | `AUDIT_LOG.md` § 2026-08-26 (Fase 77) |
| Migración de stack — Fase 76: se corrige el ROADMAP para reflejar que Sprint O ya cerró el rediseño de `/settings`, en vez de dejarlo como "pendiente" | El texto original predataba la ejecución real de Sprint O (2026-07-28) y quedó sin corregir por más de 20 fases — la documentación debe reflejar el código actual, no un estado histórico ya superado. | `AUDIT_LOG.md` § 2026-08-26 (Fase 76) |
| Migración de stack — Fase 76: se corta `settings/favorites` a Django aunque no fuera "código muerto" (estaba plenamente funcional sobre Prisma) | No cortarlo dejaba 2 copias divergentes de `User.viewPreferences` (Postgres vía favoritos, Django vía `dashboard/card-order`) — riesgo de datos activo, mismo patrón que el bug real cerrado en la Fase 52, no solo prolijidad de migración. | `AUDIT_LOG.md` § 2026-08-26 (Fase 76) |
| Migración de stack — Fase 75: `password_min_length` se clampea a un piso de 10 en vez de hacer que Django respete el valor configurable dinámicamente | Cambio chico y localizado, sin tocar Django más allá del rango del serializer — evitar el riesgo/complejidad de un validador custom para cerrar un hallazgo que no es una vulnerabilidad. Elegido explícitamente por el usuario entre 3 opciones. | `AUDIT_LOG.md` § 2026-08-26 (Fase 75) |
| Migración de stack — Fase 75: el clamp se aplica también en la LECTURA (`get_effective_password_min_length`), no solo en la validación del `PUT` | Sin esto, una fila de `SystemConfigHistory` guardada antes de la fase con un valor menor a 10 seguiría mostrándose como vigente hasta que alguien la sobrescribiera — decisión tomada durante la implementación, no parte de la pregunta original al usuario. | `AUDIT_LOG.md` § 2026-08-26 (Fase 75) |
| Migración de stack — Fase 75: no se migran los datos históricos de `SystemConfigHistory`, se clampea en la lectura | Evita tocar datos de auditoría; el clamp en lectura ya cierra el gap funcional de inmediato sin necesitar una migración. | `AUDIT_LOG.md` § 2026-08-26 (Fase 75) |
| Migración de stack — Fase 74: se le preguntó al usuario por cuál de las 4 piezas del Centro de Configuración empezar, en vez de asumir un orden | Mezclan riesgo/tamaño/naturaleza muy distintos (limpieza de código vs. decisión de seguridad vs. proyecto de UI) — a diferencia de incrementos anteriores donde el orden era obvio por dependencia técnica. | `AUDIT_LOG.md` § 2026-08-26 (Fase 74) |
| Migración de stack — Fase 74: los 2 describe de `analytics-formulas.test.ts` que probaban `insightsEngine.ts` se eliminan sin reemplazo, no se migran a un archivo nuevo | Esa cobertura ya existe, exhaustiva, del lado Django (`test_insights_engine.py`, Fases 4j/4k) — mismo criterio que la Fase 72 al eliminar tests de lógica ya migrada. | `AUDIT_LOG.md` § 2026-08-26 (Fase 74) |
| Migración de stack — Fase 74: `RiskAlert`/`RiskAlertSeverity` se mueven a `components/kpis/types.ts` en vez de a un archivo de tipos aparte | Es su único consumidor real (`import type`) — crear un archivo nuevo solo para 2 tipos de 4 líneas sería indirección sin valor. | `AUDIT_LOG.md` § 2026-08-26 (Fase 74) |
| Migración de stack — Fase 73: RANGO_PERSONALIZADO/RANGO_MESES aplican las mismas 2 decisiones de comportamiento de la Fase 72 (falla en vez de degradar, excluye colaboradores sin id) sin volver a preguntar | Ya establecido como criterio del motor completo de Reportes Ejecutivos, no una decisión nueva por builder — consistencia entre los 3 tipos de reporte. | `AUDIT_LOG.md` § 2026-08-26 (Fase 73) |
| Migración de stack — Fase 73: `monthlyEvolution.memberSnapshots` se reconstruye en TS a partir del `members` ya remapeado, en vez de pedirle a Django que incluya identidad en cada mes | El diccionario indexado por id numérico sin identidad fue una decisión deliberada de las Fases 66/71 (evita duplicar `id`/`name`/`role` una vez por mes) — el roster resuelto del lado de Next.js ya tiene esos 3 campos. | `AUDIT_LOG.md` § 2026-08-26 (Fase 73) |
| Migración de stack — Fase 73: `workday_end_hour` degrada al default si Django falla, en vez de fallar la generación | Es un insumo heurístico de Capacidad Proyectada, no el corazón visible del reporte — mismo criterio que el TTL de caché de Nova (Fase 59), deliberadamente distinto del criterio de `ReportMemberKpi` (Fase 72). | `AUDIT_LOG.md` § 2026-08-26 (Fase 73) |
| Migración de stack — Fase 73: NO se toca `src/components/settings/registry.ts` pese a tener un `DEFAULT_WORKDAY_END_HOUR` homónimo | Duplicado deliberado y ya documentado en su propio comentario, usado solo como metadata de UI en un componente cliente — no es un consumidor del valor en tiempo de ejecución. | `AUDIT_LOG.md` § 2026-08-26 (Fase 73) |
| Migración de stack — Fase 72: si Django falla al generar un reporte, la generación FALLA (no degrada) | A diferencia del Índice Ejecutivo (agregado secundario, degrada excluyendo colaboradores), `ReportMemberKpi` es el corazón visible del reporte — mostrar la tabla vacía/incompleta silenciosamente sería peor que fallar explícitamente. Confirmado con el usuario. | `AUDIT_LOG.md` § 2026-08-26 (Fase 72) |
| Migración de stack — Fase 72: `insights`/`estadoOperativo`/`principalHallazgo` NO vienen del bundle de Django, siguen calculándose en TS | Dependen de `healthByMember`/`variableConsistencyMembers`/`equilibrioScore` del Índice Ejecutivo (mes en curso), que el bundle de Django deliberadamente no calcula desde la Fase 68 — frontera ya establecida, no una improvisación de este cutover. | `AUDIT_LOG.md` § 2026-08-26 (Fase 72) |
| Migración de stack — Fase 72: se elimina el describe `"fecha de corte"` de `buildSnapshotData.test.ts` (3 tests) en vez de mantenerlo | Probaba lógica (`asOfFechaCorte`/prorrateo) que ya no vive en esta función — esa cobertura ya existe, exhaustiva, del lado Django (`test_member_kpis.py`), verificada con datos sintéticos reales en las Fases 70/71. | `AUDIT_LOG.md` § 2026-08-26 (Fase 72) |
| Migración de stack — Fase 71: se replica fielmente (bug incluido) el desplazamiento de hora LOCAL del negocio en `monthBounds()` de TS, en vez de "arreglar" el TS o dejarlo sin tocar | El principio de esta migración es replicar el TS 1:1; producción corre en `America/Guayaquil` (confirmado por el usuario) — el quirk es real y ya afecta reportes RANGO_MESES hoy, no un artefacto de entorno. | `AUDIT_LOG.md` § 2026-08-26 (Fase 71) |
| Migración de stack — Fase 71: `_local_month_bounds` reemplaza a `_month_bounds` SOLO en `compute_range_member_kpis`/`assemble_range_team_report` | Verificado con datos sintéticos que es el único builder que pasa `monthBounds()` (TS) directamente a consultas de tareas/actividades y al prorrateo — MENSUAL/RANGO_PERSONALIZADO derivan sus límites de otras fuentes ya en UTC explícito. | `AUDIT_LOG.md` § 2026-08-26 (Fase 71) |
| Migración de stack — Fase 71: el chequeo de "rango vacío" en `compute_effective_member_bases` compara instantes originales, nunca la fecha ya ajustada por `_ts_local_period_end_date` | Comparar contra la fecha ajustada haría que la rama nunca se disparara para un colaborador cuyo `clamped_start` cae después de `period_end` — bug encontrado y corregido durante la propia implementación, antes de shippear. | `AUDIT_LOG.md` § 2026-08-26 (Fase 71) |
| Migración de stack — Fase 70: se corrige el bug de redondeo (`round_half_up`) en TODO el backend (217 sitios, 32 archivos), no solo en `apps.reports` | 157 usos de `round(` en `apps/analytics/*.py` mirrorean un `Math.round()` de TS y ya sirven tráfico real desde la Fase 47 — acotar el fix a `apps.reports` habría dejado el bug activo en producción. | `AUDIT_LOG.md` § 2026-08-26 (Fase 70) |
| Migración de stack — Fase 70: NO se corrige el quirk de conteo de días hábiles en rangos multi-mes (Causa raíz B) | Es una divergencia de comportamiento real entre el TS en producción y el port Python, no un bug de puerto — decidir cuál lado "arreglar" es una decisión de producto/datos, no una corrección mecánica. | `AUDIT_LOG.md` § 2026-08-26 (Fase 70) |
| Migración de stack — Fase 70: se verifica con datos SINTÉTICOS equivalentes en Postgres y Django (no datos reales) | El entorno de desarrollo no tiene Postgres configurado ni datos reales en ninguna BD — confirmado antes de proceder, no asumido. | `AUDIT_LOG.md` § 2026-08-26 (Fase 70) |
| Migración de stack — Fase 69: `compute_range_ranking` prioriza `completed_pct` sobre `score` (orden INVERTIDO respecto a `compute_monthly_ranking`) | Verificado línea por línea contra `buildRangeSnapshotData` — no es un error de copia, el TS original ordena distinto para el builder de rango. | `AUDIT_LOG.md` § 2026-08-26 (Fase 69) |
| Migración de stack — Fase 69: `compute_range_alerts` se dispara por mayoría de meses ACTIVOS afectados, no por un umbral de un solo período | Réplica de la lógica real de `buildRangeSnapshotData` (`threshold = Math.ceil(activeMSnaps.length / 2)`) — distinta a propósito de `compute_team_alerts` (umbral fijo, builder mensual/personalizado). | `AUDIT_LOG.md` § 2026-08-26 (Fase 69) |
| Migración de stack — Fase 69: `assemble_custom_range_team_report` reutiliza `compute_monthly_ranking`/`compute_team_alerts` de la Fase 68 sin duplicarlas | `buildCustomRangeSnapshotData` usa exactamente el mismo orden de ranking y el mismo umbral de alertas que el builder MENSUAL, verificado línea por línea contra el TS. | `AUDIT_LOG.md` § 2026-08-26 (Fase 69) |
| Migración de stack — Fase 68: el bundle de `monthly-team-kpis/` NO incluye Índice Ejecutivo, Predictivo, NOVA ni `estadoOperativo`/`principalHallazgo` por miembro | Las 4 piezas dependen de motores/llamadas aparte (Índice Ejecutivo, `predictionEngine.ts`, NOVA) que están fuera del alcance de `member_kpis.py`/`insights.py` — se documentan como límite explícito del bundle, no se fuerzan ni se elimina su cálculo en TS. | `AUDIT_LOG.md` § 2026-08-26 (Fase 68) |
| Migración de stack — Fase 68: `team_report.py` SÍ resuelve `id`/`name`/`role` desde el propio `User` de Django, a diferencia de `member_kpis.py` | `compute_findings`/`compute_recommendations`/`compute_team_insights` necesitan el nombre en el texto de sus reglas; Django ya tiene su propia tabla de usuarios completa (migrada), no depende del roster de Prisma para esto. | `AUDIT_LOG.md` § 2026-08-26 (Fase 68) |
| Migración de stack — Fase 68: `POST /reports/executive/monthly-team-kpis/` es `POST`, no `GET` con query params | Un roster de 50+ colaboradores supera cómodamente el límite práctico de un query string — mismo criterio que `POST /analytics/simulate/<id>/`. | `AUDIT_LOG.md` § 2026-08-26 (Fase 68) |
| Migración de stack — Fase 67: `insights.py` es un módulo Django separado de `member_kpis.py`, no una extensión | Mismo nombre y separación de responsabilidades que el TS (`reportInsights.ts` interpreta, `member_kpis.py`/cálculo calcula) — `insights.py` nunca recalcula un KPI ni usa IA. | `AUDIT_LOG.md` § 2026-08-26 (Fase 67) |
| Migración de stack — Fase 67: NO se corrige la divergencia `now` vs. `period_end` en `compute_effective_member_bases` (Fase 64) al compararla contra el TS | La rama "sin historial" que dispara esa diferencia es inalcanzable en Django (`User.created_at` siempre poblado) — reabrir 3 sub-fases ya verificadas sin un caso real no se justifica. | `AUDIT_LOG.md` § 2026-08-26 (Fase 67) |
| Migración de stack — Fase 67: `avg_carga_pct` de `compute_team_monthly_snapshots` promedia TODO el roster, no solo los miembros activos (a diferencia de `avg_cumplimiento`) | Asimetría real del TS original (`reportInsights.ts` líneas 132-134), verificada línea por línea — se preserva y se documenta explícitamente, no se "corrige" a ser consistente. | `AUDIT_LOG.md` § 2026-08-26 (Fase 67) |
| Migración de stack — Fase 66: Sub-fase 3 se acota a `monthSnapshots`/`aggregatedMembers`, sin portar el resto de `buildRangeSnapshotData` (cuadrante de riesgo, hallazgos, recomendaciones, insights, alertas) | Ese resto depende de `src/lib/reportInsights.ts` (538 líneas, sin portar) — mismo criterio de riesgo mínimo que las Sub-fases 1/2, en vez de portar de una vez una porción bastante más grande y heterogénea. | `AUDIT_LOG.md` § 2026-08-26 (Fase 66) |
| Migración de stack — Fase 66: `month_snapshots[i]["member_snapshots"]` es un dict `{user_id: {...}}`, no un array con `id`/`name`/`role` como el TS | Mismo criterio que las Sub-fases 1/2 — el roster resuelto del lado de Next.js ya tiene esos campos; tampoco se replica el "strip" de campos previo al payload HTTP porque no hay payload HTTP todavía. | `AUDIT_LOG.md` § 2026-08-26 (Fase 66) |
| Migración de stack — Fase 65: Sub-fase 2 porta el builder de RANGO PERSONALIZADO, no el de RANGO DE MESES | El de rango de meses arma un `MonthSnapshot[]` con agregación mes a mes (promedios entre meses activos) — bastante más complejo; el personalizado es estructuralmente casi idéntico al builder mensual ya portado. | `AUDIT_LOG.md` § 2026-08-26 (Fase 65) |
| Migración de stack — Fase 65: `deriveEstadoOperativo`/`computePrincipalHallazgo` se portan completos en esta sub-fase, no se difieren | Evaluados y resultaron triviales (reutilizan `classify_estado_operativo` ya portado, o son una cascada fija de comparaciones sin estado) — el builder de rango personalizado sí los necesita, a diferencia del mensual. | `AUDIT_LOG.md` § 2026-08-26 (Fase 65) |
| Migración de stack — Fase 64: Sub-fase 1 del motor de cálculo de Reportes Ejecutivos se acota al builder MENSUAL, sin wiring a ningún endpoint HTTP | Mismo criterio del port original de KPIs/Analytics (primitivas primero, HTTP después, cutover de `route.ts` mucho más tarde) — probar el cálculo en aislamiento es lo más seguro para un motor con impacto de auditoría/compliance. | `AUDIT_LOG.md` § 2026-08-25 (Fase 64) |
| Migración de stack — Fase 64: `as_of_fecha_corte`/`compute_monthly_member_kpis` usan instancias de modelo (`.only()`), no dicts (`.values()`) | `compute_completed_pct_any`/`is_task_overdue` (funciones ya portadas y reutilizadas) esperan atributos de objeto — descubierto al correr los tests por primera vez, corregido antes de dar por cerrada la sub-fase. | `AUDIT_LOG.md` § 2026-08-25 (Fase 64) |
| Migración de stack — Fase 63: se corrige formalmente la Fase 60 — `capacityForecast.ts`/`workload.ts`/`predictionEngine.ts`/`trendEngine.ts` NO son código muerto | Verificación anterior solo comprobó importadores directos desde `src/app`; la cadena transitiva real llega a `POST /api/reports/executive` vía `buildSnapshotData.ts`. | `AUDIT_LOG.md` § 2026-08-25 (Fase 63) |
| Migración de stack — Fase 63: NO se corrige la divergencia de `workday_end_hour` (Ajustes escribe en Django, `capacityForecast.ts` lee de Postgres) descubierta en esta misma fase | Su único consumidor real es parte del motor de Reportes Ejecutivos, que ya requiere un análisis dedicado de "fecha de corte" (ver Fase 57) — un parche aislado podría generar su propia inconsistencia con ese trabajo futuro. | `AUDIT_LOG.md` § 2026-08-25 (Fase 63) |
| Migración de stack — Fase 63: solo se eliminan `recoveryCenter.ts`/`deskNoteRetention.ts`/`rate-limit.ts` (3 de 10 candidatos), no `insightsEngine.ts`/`riskAlerts.ts` | Los 2 restantes tienen un `import type` compartido (`RiskAlert`) y un archivo de test compartido con `analytics.ts` (parcialmente vivo) — requieren trabajo de separación antes de poder borrarse con seguridad. | `AUDIT_LOG.md` § 2026-08-25 (Fase 63) |
| Migración de stack — Fase 62: la duración de sesión se resuelve en el caller (`auth/me/route.ts`), no dentro de `session.ts` | Evita agregar una dependencia de red a un módulo importado por prácticamente toda la app — mismo patrón que ya usaba `login/route.ts` desde la Fase 6a. | `AUDIT_LOG.md` § 2026-08-25 (Fase 62) |
| Migración de stack — Fase 62: el fallback de `session.ts` pasa a ser un piso hardcodeado (168h/720h), no se elimina el parámetro `durationHoursOverride` | Con 0 callers reales llegando sin override, se mantiene por robustez ante un caller futuro — sin consulta a Postgres ni a Django, `session.ts` queda sin dependencias de infraestructura de datos. | `AUDIT_LOG.md` § 2026-08-25 (Fase 62) |
| Migración de stack — Fase 62: `auth/me/route.ts` sigue re-emitiendo la sesión con la duración "default", nunca "recordarme" | Quirk heredado del código original (el `PATCH` nunca recibió ni propagó ese flag) — corregirlo de paso sería un cambio de comportamiento no solicitado, fuera del alcance del cutover de fuente de configuración. | `AUDIT_LOG.md` § 2026-08-25 (Fase 62) |
| Migración de stack — Fase 61: NO se corrige el validador hardcodeado `MinimumLengthValidator(min_length=10)` de Django, desconectado de `password_min_length` configurable | Cambiar el validador (estático vs. dinámico leyendo `SystemConfigHistory`) es una decisión de producto/seguridad real, no un cutover de lectura/escritura — se documenta como hallazgo pendiente. | `AUDIT_LOG.md` § 2026-08-25 (Fase 61) |
| Migración de stack — Fase 61: se eliminan `getEffectivePasswordMinLength`/`setPasswordMinLength` de `systemConfig.ts`, a diferencia de `workdayEndHour` en la Fase 60 | Confirmado con `grep` que ningún archivo (ni siquiera código muerto) sigue importándolas — sin el bloqueo de compilación que sí aplicó a `workdayEndHour`/`capacityForecast.ts`. | `AUDIT_LOG.md` § 2026-08-25 (Fase 61) |
| Migración de stack — Fase 60: se corta `workdayEndHour` pese a que la Fase 36 documentó explícitamente que NO debía cortarse | La razón original (`capacityForecast.ts` como único consumidor real, Predictive 100% Prisma) quedó obsoleta desde la Fase 48 sin que nadie actualizara el comentario — verificado con `grep` que ese archivo ya no tiene importadores reales. | `AUDIT_LOG.md` § 2026-08-25 (Fase 60) |
| Migración de stack — Fase 60: NO se eliminan `getEffectiveWorkdayEndHour`/`setWorkdayEndHour` de `systemConfig.ts` pese a no tener consumidores reales | `capacityForecast.ts` (código muerto en runtime) sigue importándolas — son parte del grafo de compilación de TypeScript; eliminarlas rompería `tsc` sin también tocar ese archivo, fuera de alcance de esta fase. | `AUDIT_LOG.md` § 2026-08-25 (Fase 60) |
| Migración de stack — Fase 59: se elige `nova-cache` como primer cutover de Centro de Configuración (de ~9-12 candidatos) | Único valor con solo 2 consumidores reales, backend Django ya completo desde la Fase 34 — el más aislado y de menor riesgo para arrancar. | `AUDIT_LOG.md` § 2026-08-25 (Fase 59) |
| Migración de stack — Fase 59: nuevo `djangoNovaCacheConfig.ts`, no se reutiliza `systemConfig.ts` para el fallback | Importar `systemConfig.ts` solo por la constante de default habría mantenido acoplamiento a un módulo que esta fase busca dejar de usar para este valor. | `AUDIT_LOG.md` § 2026-08-25 (Fase 59) |
| Migración de stack — Fase 59: se elimina (no se comenta ni retiene) el código muerto de `getEffectiveNovaCacheTtlMinutes`/`setNovaCacheTtlMinutes` en `systemConfig.ts` | Confirmado con `grep` que no queda ningún consumidor real tras el cutover — mismo criterio del resto de la migración para código verificadamente muerto. | `AUDIT_LOG.md` § 2026-08-25 (Fase 59) |
| Migración de stack — Fase 58: el cálculo de embeddings (`@xenova/transformers`) queda permanentemente en TypeScript, no se porta a Python | Corre en proceso dentro de Node.js (no es una llamada a una API externa) — no hay ninguna razón técnica para portarlo, mismo criterio ya aplicado a Groq. | `AUDIT_LOG.md` § 2026-08-25 (Fase 58) |
| Migración de stack — Fase 58: solo se recompone la PERSISTENCIA de documentos/chunks, el PDF sigue en GitHub y el procesamiento (descarga/extracción/chunking/embeddings) sigue en `githubDocuments.ts` | Mismo patrón que Reportes Ejecutivos (Fase 56) — Django solo guarda el resultado ya calculado, evita portar lógica que no tiene ningún problema real que resolver. | `AUDIT_LOG.md` § 2026-08-25 (Fase 58) |
| Migración de stack — Fase 58: `buildTaskContext`/`buildTeamContext` de `assistant/chat/route.ts` se recomponen sobre Django (no solo se corta el CRUD de documentos) | Seguían usando Prisma directo para Tareas/Equipo, datos ya desactualizados desde las Fases 3a/46 — cerrar esa staleness en el mismo cambio evita dejarla para una fase futura. | `AUDIT_LOG.md` § 2026-08-25 (Fase 58) |
| Migración de stack — Fase 58: `buildTaskContext` responde 500 (no degrada a "sin tareas") cuando Django no tiene sesión disponible | Preserva el comportamiento observable del Prisma original ante un fallo real de datos — degradar silenciosamente habría sido una respuesta incorrecta de Nova, peor que un error explícito. | `AUDIT_LOG.md` § 2026-08-25 (Fase 58) |
| Migración de stack — Fase 58: se agrega `github_sha` a `KnowledgeDocumentSerializer` (lectura), aditivo respecto al TS legacy | `DELETE .../[id]/route.ts` lo necesita para borrar el archivo de GitHub antes de eliminar el registro — el TS original leía el registro Prisma completo sin `select`, así que nunca tuvo que declararlo explícitamente. | `AUDIT_LOG.md` § 2026-08-25 (Fase 58) |
| Migración de stack — Fase 57: se acota el alcance a SOLO el Índice Ejecutivo, dejando `ReportMemberKpi` (la tabla/ranking del reporte) sin tocar | Solo el Índice Ejecutivo tiene equivalente limpio ya portado a Django (`computePerformanceScore`/`computeHealthScore`); `ReportMemberKpi` es lógica propia sin equivalente, verificar si `/kpis/<id>/` sirve requiere su propia investigación campo por campo. | `AUDIT_LOG.md` § 2026-08-25 (Fase 57) |
| Migración de stack — Fase 57: nueva vista `GET /reports/user-lookup/` vive en `apps/reports/`, no en `apps/users/` | Es un concern específico de Reportes Ejecutivos (su único consumidor) — mismo criterio de modularidad que el resto del backend (`ver docstring de UserAdminViewSet` vs. `self_service_views.py`). | `AUDIT_LOG.md` § 2026-08-25 (Fase 57) |
| Migración de stack — Fase 57: 1 llamada a `/analytics/<id>/` por colaborador en vez de 2 llamadas separadas (Performance Score + Equilibrio Operativo) | El bundle completo (Fase 4m) ya trae ambos valores juntos — 2 round-trips habría sido una traducción mecánica innecesaria del diseño original. | `AUDIT_LOG.md` § 2026-08-25 (Fase 57) |
| Migración de stack — Fase 57: colaboradores sin id de Django resuelto se excluyen del promedio del Índice Ejecutivo, no bloquean la generación del reporte | Depende de que `migrate_users_from_postgres` haya corrido (postergada) — fallar toda la generación por un colaborador sin resolver sería una regresión real. | `AUDIT_LOG.md` § 2026-08-25 (Fase 57) |
| Migración de stack — Fase 56: se cierra la staleness de Reportes Ejecutivos SIN portar el motor de cálculo (`buildSnapshotData.ts`, 1183 líneas) | El modelo Django ya existe desde la Fase 8; solo hacía falta una vista de escritura que persista el resultado ya calculado por Next.js — portar el motor completo es un proyecto propio del orden de KPIs/Analytics (~13 sub-fases), innecesario para cerrar el hallazgo real. | `AUDIT_LOG.md` § 2026-08-25 (Fase 56) |
| Migración de stack — Fase 56: `generated_by`/`user` en los endpoints de creación se resuelven desde `request.user` (JWT), nunca desde el body | Evita que un cliente atribuya un reporte a otro usuario; más simple que el resto de la migración (no requiere `resolveDjangoUserId`). Mismo criterio que `reset_consent`/`reset_consent_all` (Fase 55). | `AUDIT_LOG.md` § 2026-08-25 (Fase 56) |
| Migración de stack — Fase 56: colisión de `report_id` responde 409, desactivando el `UniqueValidator` automático de DRF | Next.js necesita distinguir "colisión, reintentar con otro id" de "400: dato inválido, no reintentar" — el `UniqueValidator` por defecto habría devuelto 400 en ambos casos. | `AUDIT_LOG.md` § 2026-08-25 (Fase 56) |
| Migración de stack — Fase 55 (cierre): `tasks/page.tsx` pasa a resolver `fetchDjangoCurrentUserId()` en vez de `session.userId` | El gap explícito documentado desde la Fase 3a existía justamente porque `view-preferences` seguía en Prisma — al cortarlo en este mismo cambio, la condición que bloqueaba la corrección desapareció; cerrar la interdependencia completa en el mismo cambio, mismo criterio que la Fase 38/42/54. | `AUDIT_LOG.md` § 2026-08-25 (Fase 55, cierre) |
| Migración de stack — Fase 55: `UserViewPreferencesView.GET` es aditivo (sin equivalente en el TS legacy) | `tasks/page.tsx` necesita leer el array completo de `view_preferences` desde Django — el TS legacy nunca tuvo esta ruta como `GET`, así que no hay comportamiento legacy que replicar, es una extensión necesaria para cerrar la interdependencia de la Fase 3a. | `AUDIT_LOG.md` § 2026-08-25 (Fase 55, cierre) |
| Migración de stack — Fase 55: no se escriben las vistas Django nuevas de `view-preferences`/`activityFormat` sin poder correr `pytest` | SQL Server no accesible en esta sesión (`localhost:14330` sin listener TCP, Docker tampoco disponible) — escribir backend sin verificarlo rompería el estándar de "verificado con pytest" que sostiene esta migración entera. **Corrección posterior en la misma sesión: el diagnóstico era incorrecto — `localhost:14330` sí era accesible, era el contenedor Docker correcto del proyecto, no detectado porque Docker no aparecía activo en el primer chequeo.** | `AUDIT_LOG.md` § 2026-08-25 (Fase 55, cierre) |
| Migración de stack — Fase 55: `reset-consent`/`reset-consent-all` conservan `canManageUsers`/`canManageTargetUser` en TS en vez de delegar 100% a Django | Mismo criterio que `reset-password/route.ts` (Fase 2, único sibling ya cortado de `UserAdminViewSet`) — el módulo de administración de usuarios prefiere defensa en profundidad sobre el catálogo de permisos de Django. | `AUDIT_LOG.md` § 2026-08-25 (Fase 55) |
| Migración de stack — Fase 55: `view-preferences/route.ts` (bug de reemplazo completo del array, sin fusionar otras claves de prefijo) queda sin corregir | Bug preexistente del TS legacy, no introducido por esta migración — se replicará fielmente al cortarlo, corregirlo de paso cambiaría comportamiento sin que se pidiera explícitamente. | `AUDIT_LOG.md` § 2026-08-25 (Fase 55) |
| Migración de stack — Fase 54: se corrigen los 6 usos de `currentUserId` (cuid) en `MyKpisModule.tsx`, no solo `NovaInsightsCard` | Misma causa raíz, mismo cambio de una línea en cada uno — dejar 4 paneles con el mismo bug de id activo habría sido inconsistente dentro del mismo archivo. Confirmado explícitamente con el usuario. | `AUDIT_LOG.md` § 2026-08-24 (Fase 54) |
| Migración de stack — Fase 54: `nova-insights/route.ts` elimina la re-implementación manual de visibilidad jerárquica, se delega 100% a `AnalyticsBundleView`/`KpiUserView` (Django) | La misma regla ya vive portada en el backend desde la Fase 16/4b — mismo criterio que el resto de `analytics/*` ya cortadas, evita 2 copias de la regla que podrían divergir. | `AUDIT_LOG.md` § 2026-08-24 (Fase 54) |
| Migración de stack — Fase 54: `nova-message` degrada a un saludo genérico (200) si Django no está disponible, en vez del patrón de 401 del resto de la migración | Única ruta puramente cosmética (saludo del Dashboard) — antes del cutover nunca fallaba por infraestructura; el 401 estándar habría sido una regresión de UX visible por algo no resoluble desde esa pantalla. | `AUDIT_LOG.md` § 2026-08-24 (Fase 54) |
| Migración de stack — Fase 53: `settings/config-history`/`restore-default` conservan la validación de `keys`/`defaults` y el chequeo de rol ADMINISTRADOR en el `route.ts`, pese a que Django ya los valida también | Evita un round-trip a Django para requests inválidos — mismo criterio que `role-targets` (Fase 47) y `holidays`/`leave-records` (Fase 52); es defensa en profundidad, no una duplicación innecesaria. | `AUDIT_LOG.md` § 2026-08-24 (Fase 53) |
| Migración de stack — Fase 53: se corrige el párrafo desactualizado de `docs/ROADMAP.md` § Planificado (punto 14) en el mismo cambio, no como fase aparte | El párrafo describía como pendientes 5 cutovers ya cerrados desde las Fases 41-44 — dejarlo así habría confundido la próxima sesión sobre qué falta realmente de "Decommission de Postgres". | `AUDIT_LOG.md` § 2026-08-24 (Fase 53) |
| Migración de stack — Fase 52: se corrige `HolidayListView.get` (backend) para que NO exija ADMINISTRADOR | El `route.ts` real (fuente de verdad) nunca restringió `GET` — el docstring/test de la Fase 29 afirmaban lo contrario por error. Cortar sin corregir habría introducido una regresión real de permisos. | `AUDIT_LOG.md` § 2026-08-24 (Fase 52) |
| Migración de stack — Fase 52: `leave-records`/`special-status`/`workload-config`/`kpi-start-date` traducen camelCase↔snake_case a mano, sin un mapper genérico | Los campos tienen nombres distintos entre React y los `Serializer` de Django, y la dirección de la respuesta varía por endpoint (algunos ya en camelCase, otros no) — mismo criterio que `meetings` (Fase 42). | `AUDIT_LOG.md` § 2026-08-24 (Fase 52) |
| Migración de stack — Fase 51: `settings/welcome-message` se corta EN EL MISMO CAMBIO que `GET /api/dashboard`, no en una fase separada | El Dashboard ya leía este mensaje de Django antes de cortar Ajustes — dejarlo en Prisma habría hecho que cualquier mensaje configurado por un Administrador dejara de mostrarse (divergencia nueva, no preexistente). Mismo criterio que la Fase 38. | `AUDIT_LOG.md` § 2026-08-24 (Fase 51) |
| Migración de stack — Fase 51: `workload-config`/`holidays`/`leave-records`/`special-status`/`kpi-start-date` NO se cortan pese a que Dashboard ya se cortó | Su divergencia (Ajustes en Postgres vs. Analytics/Dashboard en Django) es preexistente desde la Fase 4m, no algo introducido por el cutover de Dashboard — corregirla es un alcance mayor, candidato a una fase dedicada. | `AUDIT_LOG.md` § 2026-08-24 (Fase 51) |
| Migración de stack — Fase 50: `DELETE /api/desk-notes/[id]` usa `extractDjangoFlatErrorMessage`, no un mensaje 409 genérico | El `RecoveryError` de Django varía en contenido real según la causa ("ya en la papelera" vs. "retención expirada" vs. "solo notas archivadas") — un mensaje genérico perdería información real para el usuario, mismo criterio que `restore`/`permanent` de Proyectos (Fase 39). | `AUDIT_LOG.md` § 2026-08-24 (Fase 50) |
| Migración de stack — Fase 50: `src/lib/recoveryCenter.ts` queda sin importadores tras el cutover, no se elimina | Fuera de alcance de un cutover de `route.ts` — mismo criterio que `saveAttachment`/`AttachmentError` (Fase 43). | `AUDIT_LOG.md` § 2026-08-24 (Fase 50) |
| Migración de stack — Fase 49: `role-targets`/`role-compatibility` se cortan pese a que su único caller TS (`runAnalyticsPipeline`/`computeTeamRecommendations`) es código muerto | Cortar no es cosmético — hasta ahora, editarlos desde Ajustes escribía en Postgres sin efecto en ningún motor (ni el TS muerto ni Django); redirigir a Django devuelve efecto real a la UI de Ajustes. | `AUDIT_LOG.md` § 2026-08-24 (Fase 49) |
| Migración de stack — Fase 49: los 12 endpoints de `settings/*` restantes NO se cortan pese a que Analytics/Predictive ya están 100% Django | Su consumidor real no es Analytics/Predictive en sí, sino Dashboard/Nova Insights/Reportes Ejecutivos, que TODAVÍA envuelven `workload.ts`/`analytics.ts` en TypeScript sin pasar por Django — bloqueados por un motivo distinto al original, no por Analytics/Predictive. | `AUDIT_LOG.md` § 2026-08-24 (Fase 49) |
| Migración de stack — Fase 49: `system-info` muestra `version`/`commitSha` del backend Django, no de `package.json` de Next.js | 2 aplicaciones distintas en esta migración dual-stack, cada una con su propio versionado — más honesto que seguir mostrando la versión de Next.js en un panel que ya lee de Django. | `AUDIT_LOG.md` § 2026-08-24 (Fase 49) |
| Migración de stack — Fase 48: `inteligencia-preventiva/page.tsx` pasa a `resolveDjangoUserId(session)` en vez de `session.userId` | 4 de las 9 rutas de Inteligencia Preventiva (`predictions`/`trend`/`alerts`/`simulate/[userId]`) y el campo `fromUserId` de `simulate/redistribute` ya esperan el id numérico de Django — mismo puente de la Fase 40, ya usado por Notificaciones/Reuniones/Ideas/Equipo. | `AUDIT_LOG.md` § 2026-08-24 (Fase 48) |
| Migración de stack — Fase 48: el bug de `ScenarioSimulatorPanel.tsx` (`m.id` en vez de `m.userId` en el selector de redistribución) NO se corrige | Preexistente a esta migración (ya fallaba con la ruta Prisma original, mismo nombre de campo) — fuera de alcance de un cutover que preserva el contrato exacto de la ruta original. | `AUDIT_LOG.md` § 2026-08-24 (Fase 48) |
| Migración de stack — Fase 47: 13 rutas de Analytics/KPIs se cortan como reenvío directo, sin re-implementar lógica de negocio | El backend (`apps.analytics`) ya estaba verificado campo por campo desde las Fases 16-24 — el mismo template mínimo de `analytics/[userId]`/`kpis/[userId]` (Fase 4m) aplica sin cambios a las 13. | `AUDIT_LOG.md` § 2026-08-24 (Fase 47) |
| Migración de stack — Fase 47: se agregó test coverage nueva para las 10 rutas `analytics/*` granulares, que nunca la tuvieron | Mismo criterio que la Fase 39 (Proyectos) — cerrar el gap en vez de heredarlo; la lógica de negocio ya la cubre la suite de Django. | `AUDIT_LOG.md` § 2026-08-24 (Fase 47) |
| Migración de stack — Fase 46: `/api/team` y `/api/team/[userId]/tasks` se cortan JUNTOS, en la misma fase | `TeamModule.tsx` encadena el `id` de uno como parámetro del otro — cortar solo uno rompía esa llamada (id numérico vs. `cuid`). Mismo patrón que Fase 42. | `AUDIT_LOG.md` § 2026-08-24 (Fase 46) |
| Migración de stack — Fase 45: `my-data` (exportación LOPD) NO complementa con Prisma los 5 campos de `usuario` ausentes en Django | El gap ya está documentado y aceptado en el backend (Fase 6b/11) — mezclar una segunda fuente de datos para 5 campos cosméticos introduciría su propia inconsistencia, a cambio de nada frente a tener Tareas/Reuniones/Ideas completas y actuales. | `AUDIT_LOG.md` § 2026-08-24 (Fase 45) |
| Migración de stack — Fase 44: la sección de Comunicados Y Reuniones de `GET /api/dashboard` se cortan junto con el CRUD de Comunicados | El widget de Comunicados lee el listado del bundle de Dashboard (Postgres), no de `/api/announcements` — cortar solo el CRUD dejaba publicar/eliminar pareciendo fallar. Reuniones en el mismo bundle tenía el mismo problema desde su propio cutover (Fase 42), mismo archivo, mismo arreglo. | `AUDIT_LOG.md` § 2026-08-21 (Fase 44) |
| Migración de stack — Fase 44: el resto del bundle de Dashboard (Tareas/Comentarios/Actividades/Proyectos) NO se corta | Combina datos crudos con motores de cálculo (`computeCargaTiempo`/Analytics/Workload) todavía 100% TS — cortarlo depende de una decisión de arquitectura mucho más grande, fuera de alcance de este cutover puntual. | `AUDIT_LOG.md` § 2026-08-21 (Fase 44) |
| Migración de stack — Fase 43: `mejora-continua/page.tsx` se corta junto con `/api/ideas/**` | Consulta Prisma directamente para el listado inicial (SSR), mismo patrón que `tasks/page.tsx` (Fase 3a) — dejarla sin tocar desincronizaría el primer render del resto, ya en Django. | `AUDIT_LOG.md` § 2026-08-21 (Fase 43) |
| Migración de stack — Fase 43: `saveAttachment`/`AttachmentError` quedan sin caller, no se eliminan | Ideas era su último consumidor — fuera de alcance de esta fase, mismo criterio que `rate-limit.ts` (Fase 41). | `AUDIT_LOG.md` § 2026-08-21 (Fase 43) |
| Migración de stack — Fase 42: `users/assignable` y Reuniones se cortan JUNTOS, en la misma fase | Interdependientes: el selector de invitados de Reuniones consume `users/assignable` — cortar uno sin el otro rompe el flujo de invitación en un sentido o el otro. | `AUDIT_LOG.md` § 2026-08-21 (Fase 42) |
| Migración de stack — Fase 41: `NotificationBell` recibe `session.djangoUserId`, no el `cuid`, vía un nuevo hilo de props (`layout.tsx` → `AppShell` → `Topbar`) | `taskAssignedToId` es el id numérico de Django (Tareas cutover) — comparar contra el `cuid` habría roto silenciosamente la navegación "¿es mi tarea?". `ThemeToggle`, en el mismo `Topbar`, sigue con el `cuid` sin cambios. | `AUDIT_LOG.md` § 2026-08-21 (Fase 41) |
| Migración de stack — Fase 41: NO se agrega la notificación de "tarea asignada" en `TaskService.create_task` | Es un gap preexistente desde la Fase 3a, en `apps.tasks`, no en `apps.notifications` — fuera de alcance de un cutover de lectura. | `AUDIT_LOG.md` § 2026-08-21 (Fase 41) |
| Migración de stack — Fase 40: `session.djangoUserId` se agrega como campo ADICIONAL, no reemplaza `session.userId` | Reemplazar `userId` (cuid de Postgres) rompería todo módulo todavía no cutover (`prisma.*(where: { id: session.userId })`) — Postgres sigue indexado por cuid, no por id de Django. | `AUDIT_LOG.md` § 2026-08-21 (Fase 40) |
| Migración de stack — Fase 40: `resolveDjangoUserId` cae a `/auth/me/` si falta el campo, en vez de exigir re-login | Sesiones ya emitidas antes de esta fase no tienen `djangoUserId` (JWT sin validación de schema en runtime) — forzar re-login a todos los usuarios activos es innecesario cuando el fallback de una sola llamada basta. | `AUDIT_LOG.md` § 2026-08-21 (Fase 40) |
| Migración de stack — Fase 39: `restore`/`permanent` de Proyectos usan `extractDjangoFlatErrorMessage`, NO `extractDjangoProjectErrorMessage` | Construyen su `Response` de error a mano (`{"error": "mensaje"}`, contrato plano) para preservar un orden de chequeos 404→409→403 — el extractor de contrato anidado habría perdido el mensaje real y caído siempre al fallback genérico. | `AUDIT_LOG.md` § 2026-08-21 (Fase 39) |
| Migración de stack — Fase 39: se agregó test coverage nueva para Proyectos pese a que el resto del módulo no tiene ninguna | Cerrar activamente un gap de cobertura es preferible a heredarlo — igualar el rigor ya aplicado en cada fase de esta sesión. | `AUDIT_LOG.md` § 2026-08-21 (Fase 39) |
| Migración de stack — Fase 38: `users/[id]/theme` se corta resolviendo el id numérico de Django vía `/auth/me/`, sin crear un endpoint dedicado | `UserPublicSerializer` ya incluía `"id"` sin usarlo del lado Next.js — agregar `"theme"` al mismo serializer y hacer un `GET /auth/me/` previo evita ampliar la superficie de `UserThemeView` (que exige `pk == request.user.id`). | `AUDIT_LOG.md` § 2026-08-21 (Fase 38) |
| Migración de stack — Fase 38: `layout.tsx` (lectura de tema) y `users/[id]/theme` (escritura) se cortan JUNTOS, no por separado | A diferencia de otras fases, acá ambos lados estaban en Postgres y funcionaban correctamente — cortar solo uno habría introducido un bug de staleness nuevo, no cerrado uno existente. | `AUDIT_LOG.md` § 2026-08-21 (Fase 38) |
| Migración de stack — Fase 37: `users/assignable`/`users/[id]/theme`/`view-preferences` NO se cortan todavía | Requieren reconciliar el `cuid` de Postgres (`session.userId`) con el id numérico de Django (`request.user.id`) — gap ya documentado desde la Fase 3a en `tasks/page.tsx`, sin programar. Cortarlos ahora rompería comparaciones de identidad en otros módulos. | `AUDIT_LOG.md` § 2026-08-21 (Fase 37) |
| Migración de stack — Fase 37: `profile/badges`/`activities/day-schedule` se cortan a Django sin ningún mapeo de campos | Ambos ya devuelven JSON en camelCase idéntico al contrato TS (`compute_and_persist_badges`/`DayScheduleView`, Fases 26/27) — el `route.ts` queda como reenvío directo. | `AUDIT_LOG.md` § 2026-08-21 (Fase 37) |
| Migración de stack — Fase 36: `seguridad-config`/`trabajo-avanzado` se cortan a Django PARCIALMENTE, campo por campo | En cada caso, 1 de los campos del mismo endpoint todavía tiene su consumidor real en TS/Prisma (`passwordMinLength` en `auth/change-password`, `workdayEndHour` en `capacityForecast.ts`) — cortar el endpoint completo habría dejado ese campo sin ningún lugar administrable. | `AUDIT_LOG.md` § 2026-08-21 (Fase 36) |
| Migración de stack — Fase 36: `favorites`/`documentation`/`config-history`/`system-info` NO se cortan pese a estar en `settings/*` | `favorites` comparte `User.view_preferences` con `dashboard/card-order` (sin cutover) — riesgo de divergencia entre 2 copias del mismo array; `documentation` lee Markdown del filesystem (cortar sería regresión); `config-history`/`system-info` mezclan claves/entidades ya-Django con otras todavía-Prisma. | `AUDIT_LOG.md` § 2026-08-21 (Fase 36) |
| Migración de stack — Fase 35: `notification-rules` se porta solo como configuración, sin reconectar `CommentService.create_comment`/`RETROACTIVE_NOTIFY_ROLES` | Los defaults ya coinciden exactamente con el comportamiento hardcodeado actual — conectar los consumidores reales es un cambio de comportamiento genuino en Tareas (ya en producción interna), fuera de alcance de "agregar un endpoint de Ajustes". Mismo criterio que `password_min_length` (Fase 32). | `AUDIT_LOG.md` § 2026-08-21 (Fase 35) |
| Migración de stack — Fase 35: `get_effective_notification_rules` no recibe `as_of`, a diferencia de todo el resto del catálogo | Réplica fiel de `getNotificationRules` (TS) — tampoco lo tiene, siempre lee el registro vigente sin noción de "vigente en un instante pasado". | `AUDIT_LOG.md` § 2026-08-21 (Fase 35) |
| Migración de stack — Fase 34: `ClosureStatusView` vive en `apps.reports`, no en `apps.configuration`, pese al barrido de `settings/*` | El prefijo real es `reports/executive/closure-status` (fuera del catálogo de Ajustes) — la app dueña del dominio aloja la vista, mismo criterio que `activity-reasons`/`login-attempts/cleanup`. | `AUDIT_LOG.md` § 2026-08-21 (Fase 34) |
| Migración de stack — Fase 34: `closure-status/` se registra antes del catch-all `<str:report_id>/` en `apps/reports/urls.py` | El converter `str` coincidiría con `"closure-status"` como si fuera un `report_id` literal si quedara después — mismo criterio de ordenamiento ya aplicado en Analytics. | `AUDIT_LOG.md` § 2026-08-21 (Fase 34) |
| Migración de stack — Fase 33: `login-attempts/cleanup` adapta el criterio de purga a "más antiguo que la retención" a secas, sin el estado "bloqueante" del TS | El `LoginAttempt` de Django es un log por evento (Fase 6a), sin campo de bloqueo por fila — el TS opera sobre un contador agregado por IP con `blockedUntil`. Para el modelo de Django, "más antiguo que la retención (días)" ya implica "no bloqueante" (la ventana de bloqueo real es de minutos). | `AUDIT_LOG.md` § 2026-08-21 (Fase 33) |
| Migración de stack — Fase 33: la purga de `LoginAttempt` vive en `apps.authentication`, montada bajo `settings/` | Mismo patrón que `activity-reasons` (Fase 30) — la app dueña del modelo aloja la lógica, el prefijo de URL calza con el TS. | `AUDIT_LOG.md` § 2026-08-21 (Fase 33) |
| Migración de stack — Fase 32: `notification-rules` queda fuera de esta fase | Requiere hacer configurable `RETROACTIVE_NOTIFY_ROLES` (hoy hardcodeado en `apps.tasks.services`) y `commentTargets`/`firstCommentRole` (sin equivalente en Django) — un cambio de comportamiento real, no solo una superficie de configuración; se posterga a una fase dedicada. | `AUDIT_LOG.md` § 2026-08-21 (Fase 32) |
| Migración de stack — Fase 32: `password_min_length` no se conecta al flujo de cambio de contraseña de Django, solo se porta la configuración | Django usa `AUTH_PASSWORD_VALIDATORS` (umbral fijo) en `POST /auth/password/change/`; conectarlo a `SystemConfigHistory` es un cambio de comportamiento en `apps.authentication`, ya en producción interna, fuera de alcance del Centro de Configuración. | `AUDIT_LOG.md` § 2026-08-21 (Fase 32) |
| Migración de stack — Fase 32: `seguridad-config`/`trabajo-avanzado` aceptan un `PUT` de cuerpo vacío como no-op válido (200) | Réplica fiel — a diferencia de `escritorio-digital-config` (Fase 31), el TS de estas 2 rutas nunca valida "nada que guardar". | `AUDIT_LOG.md` § 2026-08-21 (Fase 32) |
| Migración de stack — Fase 31: `workload-config`/`analytics-config` se validan manualmente en la vista, no con un `Serializer` | Cuerpos dispersos con validación de rango cruzado (orden de límites, sumas de ponderación, orden de umbrales) que depende de los valores YA vigentes para campos no enviados — hay que fusionar "vigente + enviado" antes de validar, fuera del alcance natural de un `Serializer` de forma fija. | `AUDIT_LOG.md` § 2026-08-21 (Fase 31) |
| Migración de stack — Fase 31: `retention-policy/purge` (ejecución real) queda fuera de alcance, solo se porta la política (GET/PUT) | Requiere `MonthlyReport`/`KnowledgeDocument`/`DataPurgeLog`, ninguno portado — dependen de Reportes Ejecutivos y Nova-RAG, ambos fuera de alcance de la migración por ahora. | `AUDIT_LOG.md` § 2026-08-21 (Fase 31) |
| Migración de stack — Fase 30: `ActivityReason.description` se persiste como cadena vacía, nunca `None` | El campo Django (`blank=True, default=""`, Fase 3b) nunca fue nullable, a diferencia del `String?` de Prisma — asignar `None` violaría la restricción NOT NULL. | `AUDIT_LOG.md` § 2026-08-21 (Fase 30) |
| Migración de stack — Fase 30: restaurar un motivo archivado (`is_archived=False`) no reactiva `is_active` automáticamente | Asimetría real del TS, replicada tal cual — un administrador debe reactivarlo aparte si corresponde. | `AUDIT_LOG.md` § 2026-08-21 (Fase 30) |
| Migración de stack — Fase 29: "usuario no encontrado" (404) en `leave-records`/`special-status` se valida en la VISTA, no en el serializer | Ocurre después de las validaciones de forma (400) pero antes del cómputo de negocio — un serializer lo habría colapsado en un 400 genérico, perdiendo el código de estado exacto del TS. | `AUDIT_LOG.md` § 2026-08-20 (Fase 29) |
| Migración de stack — Fase 29: `holidays`/`leave-records`/`special-status` se agrupan en una fase, `activity-reasons` queda aparte | Los 3 primeros comparten el mismo patrón (modelo ya completo desde la Fase 4a, CRUD simple, ADMINISTRADOR-only) y sus docstrings ya los agrupaban como trío; `activity-reasons` vive en un prefijo distinto (`/activity-reasons/`, no `/settings/`) desde la Fase 3b, mezclarlo habría sido más confuso que separarlo. | `AUDIT_LOG.md` § 2026-08-20 (Fase 29) |
| Migración de stack — Fase 28: `ROLE_LABEL`/`ALL_ROLES` se centralizan en `apps.hierarchy.services`, retirados de `apps.tasks.services` | `role-targets`/`role-compatibility` son el segundo consumidor real de `ROLE_LABEL` — mismo criterio ya aplicado a `ROLE_LEVEL` en la Fase 9b (no triplicar una copia local). | `AUDIT_LOG.md` § 2026-08-20 (Fase 28) |
| Migración de stack — Fase 28: `can_manage_users` (`role-targets`/`role-compatibility`) es un whitelist literal de 3 roles, no el catálogo de permisos administrativo | Réplica fiel de `canManageUsers`/`CAN_MANAGE_USERS` (`src/lib/roles.ts`) — el TS nunca usó el catálogo de permisos para estas 2 rutas. | `AUDIT_LOG.md` § 2026-08-20 (Fase 28) |
| Migración de stack — Fase 28: `snooze-presets` se porta solo lectura, sin `set_snooze_presets_minutes` | El TS tiene el setter en la librería pero ningún `route.ts` lo expone (confirmado con grep) — no se porta un setter sin consumidor HTTP real. | `AUDIT_LOG.md` § 2026-08-20 (Fase 28) |
| Migración de stack — Fase 27: se cierra el gap del badge "innovador" en `apps.ideas` (documentado desde la Fase 11) en la misma fase que se porta `profile/badges`, no en una fase separada | Mismo dominio funcional (gamificación) y el campo que lo bloqueaba (`User.badges`) recién existe desde la Fase 25 — resolverlo junto evita reabrir `apps.ideas` más adelante para una sola línea. | `AUDIT_LOG.md` § 2026-08-20 (Fase 27) |
| Migración de stack — Fase 27: `compute_and_persist_badges` vive en `apps/users/badges.py`, consumido desde `apps/users/self_service_views.py` (Fase 26) | Es una operación de auto-servicio sobre el propio perfil del actor — mismo criterio de ubicación que `assignable`/`theme`. | `AUDIT_LOG.md` § 2026-08-20 (Fase 27) |
| Migración de stack — Fase 26: `users/assignable`/`users/<id>/theme` viven en `apps/users/self_service_views.py` nuevo (montado en `users/`), separado de `UserAdminViewSet` (`admin/users/`) | Auto-servicio (cualquier autenticado) vs. administración (permisos `usuarios.*`) son 2 superficies con semántica de autorización distinta sobre el mismo modelo — mezclarlas habría requerido lógica condicional dentro del mismo ViewSet. | `AUDIT_LOG.md` § 2026-08-20 (Fase 26) |
| Migración de stack — Fase 26: `repository`/`repository/<year>/<month>` y `auth/reset-password` se excluyen del barrido — ya estaban cortados a Django (Fases 3d/6c) | El propio `route.ts` de ambos ya es un adaptador delgado hacia Django en producción — no eran trabajo pendiente real. | `AUDIT_LOG.md` § 2026-08-20 (Fase 26) |
| Migración de stack — Fase 25: `User.lastLoginAt` (TS) se replica reutilizando el `last_login` nativo de `AbstractUser`, sin campo dedicado nuevo | Ninguno de los 2 se escribe nunca (TS: sin escritor real en `src/app/api`; Django: sin uso de `django.contrib.auth.login()`, auth 100% JWT manual) — mismo comportamiento observable (`None` siempre) sin columna redundante. | `AUDIT_LOG.md` § 2026-08-20 (Fase 25) |
| Migración de stack — Fase 25: `apps.announcements` se porta completo (modelo + GET/POST/DELETE), no solo el GET que el Dashboard consume | El Dashboard necesita `Announcement` de todos modos y el alcance total (3 handlers, ~90 líneas TS) es pequeño y autocontenido — portar solo lectura habría dejado un modelo sin forma de poblarse en Django. | `AUDIT_LOG.md` § 2026-08-20 (Fase 25) |
| Migración de stack — Fase 24: `get_effective_role_compatibility` sigue el mismo mecanismo JSON-en-`SystemConfigHistory` que `get_effective_role_target` (Fase 22), sin campo de modelo nuevo | Mismo patrón que `RoleTarget` en el TS — solo cambia la forma (lista de roles vs. objeto de 3 números). | `AUDIT_LOG.md` § 2026-08-20 (Fase 24) |
| Migración de stack — Fase 24: `compute_team_recommendations` se porta línea por línea como lógica nueva genuina, a diferencia de Benchmarks/`simulate` | No es una función pre-extraída anticipando el puerto — es el algoritmo greedy completo, verificado con tests dedicados para cada una de las 5 reglas de negocio documentadas en el TS (especialmente la Regla 4, dura: nunca redistribución vertical, ni con matriz configurada). | `AUDIT_LOG.md` § 2026-08-20 (Fase 24) |
| Migración de stack — Fase 23: el simulador KPI-level se porta en `kpi_simulate.py` nuevo, no en `simulate_engine.py` existente | Son 2 simuladores distintos con el mismo nombre coloquial — `simulate_engine.py` (Fase 9c) opera sobre predicciones; este opera directamente sobre Equilibrio Operativo/Performance Score, sin tocar el motor de predicción. Mezclarlos habría confundido dos dominios. | `AUDIT_LOG.md` § 2026-08-20 (Fase 23) |
| Migración de stack — Fase 23: el cuerpo del simulador KPI-level se valida con una función manual (`is_valid_scenario`), no un `Serializer` DRF | Es una unión discriminada por `type` con 8 formas de cuerpo distintas — forzarlo a un `Serializer` único habría requerido lógica polimórfica innecesaria; primer caso de este patrón en el proyecto, documentado explícitamente. | `AUDIT_LOG.md` § 2026-08-20 (Fase 23) |
| Migración de stack — Fase 22: Benchmarks Inteligente se implementa completo en una sola fase, reutilizando `get_factor_audit_history`/`closest_factor_point` ya portados en vez de crear una variante "scored" nueva | `getScoredAuditHistory`/`closestScoredPoint` (TS) son estructuralmente el mismo mecanismo que las funciones Django ya portadas en la Fase 4j/9 — la investigación previa confirmó cero gap de motor real. | `AUDIT_LOG.md` § 2026-08-20 (Fase 22) |
| Migración de stack — Fase 22: `get_effective_role_target` se porta (solo lectura), `set_role_target`/su endpoint HTTP quedan fuera de alcance | `role-targets` es uno de los 14 endpoints preexistentes de `/api/settings` (Sprint O), fuera de esta fase — el motor ya maneja correctamente "cargo sin configurar" (`None`), que es el comportamiento por defecto real hoy. | `AUDIT_LOG.md` § 2026-08-20 (Fase 22) |
| Migración de stack — Fase 22: el motor de Benchmarks nunca cruza cargos distintos que comparten `ROLE_LEVEL` (ej. Coordinador ZS vs. Analista CC, ambos nivel 2) | Filtra por grupo/cargo exacto, nunca por nivel jerárquico — verificado con un test dedicado. | `AUDIT_LOG.md` § 2026-08-20 (Fase 22) |
| Migración de stack — Fase 21: `kpis/executive` se implementa completo en una sola fase, sin dividir | La investigación previa confirmó que las 326 líneas TS son 100% orquestación sobre motor ya portado (Performance Score/Riesgo Operativo/carga/cumplimiento) — dividir habría sido granularidad artificial, a diferencia de fases previas donde el motor faltante sí justificó acotar el alcance. | `AUDIT_LOG.md` § 2026-08-20 (Fase 21) |
| Migración de stack — Fase 21: imports de `operational_risk.py`/`performance_score.py` diferidos dentro de `build_executive_dashboard_payload`, no al tope de `services.py` | Import a nivel de módulo produjo un `ImportError` real por ciclo circular (`services.py`→`operational_risk.py`→`history.py`→`services.py`) — mismo patrón ya usado para `pipeline.py` en `build_analytics_bundle_payload` (Fase 4m). | `AUDIT_LOG.md` § 2026-08-20 (Fase 21) |
| Migración de stack — Fase 20: `Notification.dedup_key` (campo nuevo) en vez de reutilizar `task_id` para el marcador de deduplicación de `notify_if_high_risk` | El TS abusa de `taskId` (`String` en Prisma) como marcador de texto libre; `task_id` en Django es un `PositiveBigIntegerField` real desde la Fase 3f, ya consumido por `GET /notifications/` — no reutilizable sin un error de tipos o una semántica confusa. | `AUDIT_LOG.md` § 2026-08-20 (Fase 20) |
| Migración de stack — Fase 20: `operational-risk/team` usa el conjunto de destinos de notificación POR DEFECTO, sin portar el override configurable de Ajustes | `get_notification_target_groups` (estático, ya portado desde la Fase 1) cubre el caso común; el override vía `SystemConfigHistory` (Sprint O) no está portado — gap documentado, no bloqueante. | `AUDIT_LOG.md` § 2026-08-20 (Fase 20) |
| Migración de stack — Fase 20: `recommendations/team` queda fuera de alcance, requiere `computeTeamRecommendations` (motor de ~100 líneas + Matriz de Compatibilidad Operativa) | Motor de redistribución completo con 5 reglas de negocio propias, no ensamblado sobre lo ya portado — mismo criterio que separó Benchmarks/`simulate`/`kpis/executive` en fases previas. | `AUDIT_LOG.md` § 2026-08-20 (Fase 20) |
| Migración de stack — Fase 19: `is_executor_group`/`get_subordinate_executor_groups` viven en `apps.hierarchy.services` como concepto separado de `is_leadership`, no fusionados | Mismo nombre conceptual, umbrales (4 vs. 3) y usos distintos — fusionarlos cambiaría el comportamiento real para COORDINADOR_NACIONAL (nivel 3), verificado con un test dedicado que confirma que JEFE_NACIONAL aparece en uno pero no en el otro para el mismo actor. | `AUDIT_LOG.md` § 2026-08-20 (Fase 19) |
| Migración de stack — Fase 19: `kpis/team`/`kpis/team-capacity` se portan, `kpis/executive` queda deferida | Las 2 primeras son 100% ensamblado sobre motor ya portado; `kpis/executive` (~326 líneas TS) tiene lógica de síntesis propia (tendencia 6 meses, ranking, bloque CEO) sustancialmente mayor, mismo criterio que separó Benchmarks/`simulate` en fases previas. | `AUDIT_LOG.md` § 2026-08-20 (Fase 19) |
| Migración de stack — Fase 18: `kpis/team`/`kpis/team-capacity`/`kpis/executive` se defieren, requieren un concepto de permiso nuevo (`isExecutorRole`, `ROLE_LEVEL>=4`) distinto de `is_leadership` (`ROLE_LEVEL>=3`) ya existente | Mismo nombre conceptual ("liderazgo"), umbral y uso distintos — portarlas de apuro habría arriesgado confundir los dos conceptos; se prefiere investigar a fondo en una fase dedicada. | `AUDIT_LOG.md` § 2026-08-20 (Fase 18) |
| Migración de stack — Fase 18: nueva app `apps.team` sin modelos propios | Ambas rutas son consultas puras sobre `Task`/`User` — mismo patrón que `apps.roles` (sin `models.py`). | `AUDIT_LOG.md` § 2026-08-20 (Fase 18) |
| Migración de stack — Fase 18: asimetría de enmascarado de email entre `GET /team/` (enmascara) y `GET /team/<id>/tasks/` (no enmascara) se replica tal cual | Comportamiento real del TS (`taskSelect` nunca aplicó `maskEmail`) — fidelidad 1:1, no se "corrige" hacia consistencia no solicitada. | `AUDIT_LOG.md` § 2026-08-20 (Fase 18) |
| Migración de stack — Fase 18: gap de enmascarado de email en `apps.projects` (ya en producción) detectado pero NO corregido | Fuera de alcance de esta fase — tocaría un módulo ya cortado sin que se pidiera; documentado como hallazgo pendiente para revisión futura. | `AUDIT_LOG.md` § 2026-08-20 (Fase 18) |
| Migración de stack — Fase 17: `history`/`target-time`/`data-quality` se portan, `diagnostics`/`simulate` quedan deferidos | Los 3 primeros son 100% ensamblado sobre motor ya portado; `diagnostics` depende de instrumentación de proceso nunca portada (`getDiagnosticsSnapshot`), `simulate` (KPI-level) es un motor de 8 escenarios sustancial, no una ruta delgada. | `AUDIT_LOG.md` § 2026-08-20 (Fase 17) |
| Migración de stack — Fase 16: solo 3 de las 16 rutas delgadas de Analytics se portan esta fase (Insights/Equilibrio/Riesgo Operativo) | Las 3 son composición pura sobre motor YA portado (`insights_engine.py` completo desde Fase 4j/4k); Benchmarks requiere un motor nuevo (`computeSmartBenchmark`/`computePersonalEvolution`), el resto queda deferido explícitamente. | `AUDIT_LOG.md` § 2026-08-20 (Fase 16) |
| Migración de stack — Fase 16: `can_view_operational_risk` es un whitelist de roles puntual, no un umbral de `role_level` | `ANALISTA_CC`/`ANALISTA_SELECCION` comparten nivel 2 con `COORDINADOR_ZS` en `ROLE_LEVEL` pero NO están en `CAN_VIEW_OPERATIONAL_RISK` del TS — mismo criterio ya establecido para `can_create_meetings` (Fase 10). | `AUDIT_LOG.md` § 2026-08-20 (Fase 16) |
| Migración de stack — Fase 16: `OperationalRiskView` chequea el whitelist de rol ANTES de buscar al usuario objetivo | Réplica exacta del orden del `route.ts` — un `user_id` inexistente con un actor sin permiso de rol da 403, no 404; verificado con tests dedicados para ambos órdenes. | `AUDIT_LOG.md` § 2026-08-20 (Fase 16) |
| Migración de stack — Fase 15: `GET/PATCH /notifications` y `PATCH /notifications/<id>` se portan sin cutover de `route.ts`, a diferencia de otros módulos ya completos | `Notification` en el TS se alimenta también de módulos que todavía no migraron a Django (Nova/Dashboard) — cortar el endpoint mostraría una lista incompleta a usuarios reales. | `AUDIT_LOG.md` § 2026-08-20 (Fase 15) |
| Migración de stack — Fase 15: `PATCH /notifications/<id>` replica el comportamiento silencioso del `updateMany` del TS (sin 404/403 para id ajeno o inexistente) | Fidelidad 1:1 — el TS no distingue "no existe" de "es de otro usuario", ambos responden `{ok:true}` sin error; no se "mejora" sin que se pida. | `AUDIT_LOG.md` § 2026-08-20 (Fase 15) |
| Migración de stack — Fase 14: Papelera de Proyectos porta el flujo completo, Notas de Escritorio Digital solo `moveToTrash` — asimetría fiel al TS | Confirmado por grep exhaustivo: no existen rutas de restaurar/listar/eliminar-definitivo para notas ni en el backend ni en el frontend TS. Decisión explícita del usuario de no "completar" el flujo de Notas. | `AUDIT_LOG.md` § 2026-08-20 (Fase 14) |
| Migración de stack — Fase 14: se incluyen ambos mecanismos de purga (`purge_expired_items` genérico y `purge_expired_archived_notes` específico de Notas), confirmados independientes | No comparten código ni tabla — cierra el gap de `purgeExpiredArchivedNotes` documentado desde la Fase 7g, decisión explícita del usuario. | `AUDIT_LOG.md` § 2026-08-20 (Fase 14) |
| Migración de stack — Fase 14: `ENTITY_REGISTRY` (dict de `EntityAdapter`) como patrón abierto/cerrado para la papelera | Puerto directo de `Record<string, EntityAdapter>` del TS — módulos futuros solo registran un adaptador, sin tocar el motor `move_to_trash`/`restore`/`delete_permanently`/`purge_expired_items`. | `AUDIT_LOG.md` § 2026-08-20 (Fase 14) |
| Migración de stack — Fase 14: `purge_expired_items()` no filtra por `entity_type` (acoplamiento incidental entre módulos) se replica, no se corrige | Comportamiento real del TS — un puerto de fidelidad 1:1 no corrige comportamiento sin que se pida; verificado con un test dedicado. | `AUDIT_LOG.md` § 2026-08-20 (Fase 14) |
| Migración de stack — Fase 14: `restore`/`permanent` de Proyectos y `destroy` de Notas hacen los chequeos 404→409→403 a mano, sin `get_permissions`/`has_object_permission` | El pipeline automático de DRF resuelve 404→403 sin lugar para un 409 intermedio — mismo patrón ya usado en Reuniones/Ideas/LOPD. | `AUDIT_LOG.md` § 2026-08-20 (Fase 14) |
| Migración de stack — Fase 14: `RecoveryItem.deleted_at` es un `DateTimeField` plano, sin `auto_now_add` | Evita el desfase de milisegundos entre el timestamp persistido por `auto_now_add` y el `timezone.now()` usado en Python para calcular `expires_at` — se asigna la misma variable a ambos. | `AUDIT_LOG.md` § 2026-08-20 (Fase 14) |
| Migración de stack — Fase 13: `reset-consent` (por usuario) usa el catálogo de permisos, `reset-consent-all` (masivo) replica el chequeo estricto literal del TS | El catálogo de `apps.permissions` ya superó `canManageUsers`/`canManageTargetUser` del TS para acciones individuales; la acción masiva/irreversible amerita el mismo chequeo hardcodeado que el TS (`IsAdministrator`, nueva clase), más estricto que cualquier permiso del catálogo. | `AUDIT_LOG.md` § 2026-08-19 (Fase 13) |
| Migración de stack — Fase 13: `AuditLog.previous_values`/`new_values` no soportan `datetime` crudo (bug real encontrado, no solo de test) | `JSONField` sin `encoder=DjangoJSONEncoder` — el driver mssql explota al serializar. Corregido con `.isoformat()` en `UserAdminService.reset_consent`; a tener en cuenta en cualquier `record_audit_event` futuro con campos de fecha. | `AUDIT_LOG.md` § 2026-08-19 (Fase 13) |
| Migración de stack — Fase 12: se investiga el alcance legal ANTES de portar código, confirmando que no hay borrado/anonimización real automatizada en Solicitudes LOPD | El ROADMAP marca el módulo "sensible legalmente" — la investigación (RAT.md, grep exhaustivo) confirmó que "eliminación de cuenta" es 100% gestión manual, sin automatización de alto riesgo que portar con cuidado especial. | `AUDIT_LOG.md` § 2026-08-19 (Fase 12) |
| Migración de stack — Fase 12: alcance limitado a `DataSubjectRequest`, el gate de consentimiento queda deferido | Decisión explícita del usuario — el gate de consentimiento es un mecanismo distinto (bloquea el render de toda la app) y tocaría el modelo `User` compartido por todo el backend. | `AUDIT_LOG.md` § 2026-08-19 (Fase 12) |
| Migración de stack — Fase 12: `export_my_data` omite campos de `User` sin equivalente en Django en vez de fabricar valores falsos | `theme`/`viewPreferences`/`badges`/`dataConsentAccepted` son gaps ya documentados en fases previas — un export de datos personales debe ser fiel a lo que existe, no inventar placeholders. | `AUDIT_LOG.md` § 2026-08-19 (Fase 12) |
| Migración de stack — Fase 11: `get_visible_idea_author_ids` replica la excepción de jerarquía de Mejora Continua tal cual (Jefe/Coordinador Nacional ven todas las ideas) | El propio TS documenta esta excepción explícitamente frente a la jerarquía general de KPIs/Analytics/Informes — se replica con el mismo comentario, no como código sin contexto. | `AUDIT_LOG.md` § 2026-08-19 (Fase 11) |
| Migración de stack — Fase 11: se cubre el caso "superusuario sin Group" en la visibilidad de ideas, que el TS no necesita cubrir | Django separa `is_superuser` de `auth.Group`; sin este chequeo explícito un superusuario recién creado vería cero ideas en vez de todas. | `AUDIT_LOG.md` § 2026-08-19 (Fase 11) |
| Migración de stack — Fase 11: la asimetría de `PATCH /ideas/[id]/status` (no filtra por visibilidad del autor, a diferencia del resto de rutas de detalle) se replica, no se corrige | Es un comportamiento real del TS legacy — un puerto de fidelidad 1:1 no corrige comportamiento sin que se pida. | `AUDIT_LOG.md` § 2026-08-19 (Fase 11) |
| Migración de stack — Fase 11: el badge "innovador" (`User.badges`) no se porta | Gamificación transversal sin campo equivalente en Django — agregarlo tocaría un modelo compartido por todo el backend por una única funcionalidad puntual. | `AUDIT_LOG.md` § 2026-08-19 (Fase 11) |
| Migración de stack — Fase 10: Otter.ai no requiere ninguna integración de API en Django | Confirmado exhaustivamente: es 100% edición manual de 3 campos en el TS legacy, sin llamada HTTP a otter.ai en ningún lado (el propio README ya lo documentaba). | `AUDIT_LOG.md` § 2026-08-19 (Fase 10) |
| Migración de stack — Fase 10: se agrega `requests` como primera dependencia HTTP saliente del backend | Sin precedente previo (NOVA/Groq nunca se portaron) — elegido sobre `httpx` por simplicidad y consistencia con el resto del stack síncrono DRF. | `AUDIT_LOG.md` § 2026-08-19 (Fase 10) |
| Migración de stack — Fase 10: `can_create_meetings` se porta como whitelist puntual de roles, no como umbral de `role_level` | Coordinador ZS comparte nivel 2 con Analista CC/Selección en `ROLE_LEVEL`, pero solo Coordinador ZS crea reuniones en el TS — un umbral numérico habría sido una divergencia funcional real. | `AUDIT_LOG.md` § 2026-08-19 (Fase 10) |
| Migración de stack — Fase 10: `MeetingInvitee.attended` se porta fiel al esquema sin implementarlo funcionalmente | Ningún endpoint/componente del TS lee o escribe este campo — es un campo "muerto" heredado del modelo Prisma; agregarle funcionalidad sería una mejora no solicitada. | `AUDIT_LOG.md` § 2026-08-19 (Fase 10) |
| Migración de stack — Fase 9c: `prediction-window` se defiere al Centro de Configuración, `simulate/*` se cierra en esta sub-fase | `prediction-window` sería el primer endpoint HTTP de `apps.configuration` — nace mejor cuando se aborde ese módulo, no como efecto colateral de Inteligencia Preventiva. | `AUDIT_LOG.md` § 2026-08-18 (Fase 9c) |
| Migración de stack — Fase 9c: `simulate_engine.py` (funciones puras) se separa de las vistas HTTP | Mismo patrón que `trend_engine.py`/`prediction_engine.py` — permite testear la matemática sin pasar por HTTP/DB. | `AUDIT_LOG.md` § 2026-08-18 (Fase 9c) |
| Migración de stack — Fase 9b: `simulate/*`/`prediction-window` se defieren a una sub-fase futura, el resto de Inteligencia Preventiva se cierra | Ambos son piezas genuinamente distintas (calculadoras "qué pasaría si" sin persistencia; `prediction-window` sería el primer endpoint HTTP de `apps.configuration`, más cerca de "Centro de Configuración" que de este módulo). | `AUDIT_LOG.md` § 2026-08-18 (Fase 9b) |
| Migración de stack — Fase 9b: `ROLE_LEVEL`/`role_level`/`is_leadership`/`can_view_team` se centralizan en `apps.hierarchy.services`, migrando desde `apps.projects.permissions` | El propio docstring de Proyectos (Fase 5a) anticipaba "se generaliza cuando aparezca un segundo consumidor real" — `team-alerts`/`team-subutilization` son ese segundo consumidor. | `AUDIT_LOG.md` § 2026-08-18 (Fase 9b) |
| Migración de stack — Fase 9b: `project-delay` se gatea por `can_view_project`, no por visibilidad jerárquica de usuario | Réplica exacta del `route.ts` original — un proyecto tiene su propio modelo de permisos, independiente de si el actor ve al responsable en la jerarquía de personas. | `AUDIT_LOG.md` § 2026-08-18 (Fase 9b) |
| Migración de stack — Fase 9a: Trend/Prediction Engine viven dentro de `apps.analytics`, no en una app `apps.predictive` nueva | Inteligencia Preventiva es una capa derivada que SOLO lee datos ya calculados por Analytics — mismo criterio que `audit_history.py`/`insights_engine.py`. | `AUDIT_LOG.md` § 2026-08-18 (Fase 9a) |
| Migración de stack — Fase 9a: se porta `prediction_engine.py` completo pero solo se exponen 2 endpoints HTTP | Evita dejar el archivo fuente parcial/inconsistente respecto al TS mientras se acota el riesgo de la sub-fase al bundle individual y al Trend Engine. | `AUDIT_LOG.md` § 2026-08-18 (Fase 9a) |
| Migración de stack — Fase 9a: Alertas Preventivas (`preventiveIntelligence.ts`) queda fuera de esta sub-fase | Depende de Trend/Prediction Engine — se prefiere cerrar y testear el motor base antes de construir la capa de priorización de alertas encima. | `AUDIT_LOG.md` § 2026-08-18 (Fase 9a) |
| Migración de stack — Fase 9a: sin capa de caché con TTL en `/predictive/predictions`/`/predictive/trend` | Réplica del gap ya aceptado en el bundle de Analytics (`cache_active: False`, Fase 4m) — evita una asimetría de comportamiento entre bundles de Django. | `AUDIT_LOG.md` § 2026-08-18 (Fase 9a) |
| Migración de stack — Fase 8: Reportes Ejecutivos se porta solo en lectura, la generación queda fuera de esta sub-fase | El motor de generación (~3200 líneas + narrativa IA vía Groq) es riesgo equivalente al Asistente LLM/RAG — el ROADMAP ya anticipaba este alcance ("snapshots ya generados, nunca recalculados"). | `AUDIT_LOG.md` § 2026-08-18 (Fase 8) |
| Migración de stack — Fase 8: `MonthlyReport` (modelo legacy) no se porta | Solo alimentó una corrida única de backfill (4 filas `LEGACY_MIGRATION`) — sin consumidor propio hoy. | `AUDIT_LOG.md` § 2026-08-18 (Fase 8) |
| Migración de stack — Fase 7g: `DELETE /api/desk-notes/[id]` se queda en Prisma, mismo patrón que Proyectos (Fase 5f) | El borrado real pasa por el Centro de Recuperación (transversal, sin portar) — cortarlo dejaría notas "perdidas" para la papelera. | `AUDIT_LOG.md` § 2026-08-18 (Fase 7g) |
| Migración de stack — Fase 7g: el adjunto de `POST /api/desk-notes` se codifica a base64 en el `route.ts`, no en el cliente | Mantiene el `multipart/form-data` existente sin tocar `NewNoteModal.tsx` — mismo principio que sostiene toda la migración (frontend sin cambios). | `AUDIT_LOG.md` § 2026-08-18 (Fase 7g) |
| Fix post-cutover: 13 tests de Vitest se reescriben (no se eliminan) tras detectar que mockeaban Prisma para rutas ya cortadas a Django | El wrapper `route.ts` sigue teniendo su propia superficie de bugs (mapeo body/respuesta, status codes) que la cobertura de Django no puede cubrir. | `AUDIT_LOG.md` § 2026-08-18 |
| Fix post-cutover: `team/[userId]/tasks/route.ts` reconstruye `taskSelect` localmente en vez de migrarse a Django | `GET /tasks/` de Django siempre filtra por `assigned_to=request.user` — no existe endpoint para ver tareas de otro usuario; portarlo es trabajo de la migración de Tareas, no de este fix. | `AUDIT_LOG.md` § 2026-08-18 |
| Migración de stack — Fase 7f: `desk/today` y `desk/search` se agrupan en una sola sub-fase | Ambos son de solo lectura, sin escritura ni reglas de negocio propias — separarlos habría sido granularidad artificial. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7f) |
| Migración de stack — Fase 7f: `desk/today` reutiliza `business_calendar_day`/`business_day_real_range` de `apps.tasks.business_time` en vez de reimplementar `todayRange` | Son la función 1:1 que ya consume `todayRange()` en el TS — reimplementarlas habría duplicado lógica ya verificada. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7f) |
| Migración de stack — Fase 7f: `desk/today`/`desk/search` son `APIView` simples, no `ViewSet` | No tienen un modelo propio detrás — son agregaciones de lectura sobre modelos con su propio `ViewSet` ya existente. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7f) |
| Migración de stack — Fase 7e: `convert-to-reminder` exclusivo del destinatario, no del remitente | El `route.ts` original chequea `recipientId`, no "sender o recipient" — es quien recibió la nota quien decide agendarse un recordatorio. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7e) |
| Migración de stack — Fase 7e: `PersonalReminder` gana adjuntos SIN endpoint de subida/descarga propio | Verificado contra el TS completo: no existe ningún endpoint que suba/descargue el adjunto de un recordatorio de forma independiente — solo llega por copia. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7e) |
| Migración de stack — Fase 7e: título truncado a 149 (no 150) caracteres antes del "…" | El TS trunca a 150 + "…" = 151, que cabe en el `String` sin límite de Postgres pero no en `CharField(max_length=150)`/MSSQL. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7e) |
| Migración de stack — Fase 7d: solo Adjuntos de Notas en esta sub-fase, Adjuntos de Recordatorios se difiere junto con `convert-to-reminder` | `PersonalReminder` en el TS solo recibe adjunto por copia al convertir Nota→Recordatorio — sin esa conversión portada, no hay upload propio que aislar. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7d) |
| Migración de stack — Fase 7d: adjuntos se mandan como data URL en JSON, no `multipart/form-data` | Mismo patrón que `ProjectDocumentUploadSerializer.file_data` (Fase 5d) — un solo estilo de contrato en toda la API Django, sin consumidor real todavía que fije lo contrario. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7d) |
| Migración de stack — Fase 7d: `GET .../attachment` chequea 404 antes que 403, a propósito no usa `self.get_object()` | Réplica exacta del orden de validación del TS (`!attachmentData` antes que la pertenencia a la conversación). | `AUDIT_LOG.md` § 2026-08-17 (Fase 7d) |
| Migración de stack — Fase 7c: `convert-to-task` reutiliza `TaskService.create_task` en vez de crear el `Task` directo | Evita duplicar el cálculo de `progress`/`completed_at` a partir de `status` — divergería en silencio si `TaskService.create_task` cambia a futuro. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7c) |
| Migración de stack — Fase 7c: 403 explícito (no 404) para quien no es el dueño en `convert-to-task`, a diferencia del resto de `DeskReminderViewSet` | El `route.ts` original es un endpoint aislado que sí distingue "existe, no es tuyo" de "no existe" — única acción que no usa `self.get_object()` (que filtra por dueño). | `AUDIT_LOG.md` § 2026-08-17 (Fase 7c) |
| Migración de stack — Fase 7c: sin adjunto en la descripción de la tarea creada | `PersonalReminder` todavía no tiene adjuntos en Django (sub-fase propia, sin planificar en detalle) — gap aceptado, se resuelve solo cuando se porten. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7c) |
| Migración de stack — Fase 7b: `DELETE` de recordatorios sí se porta (a diferencia de Notas) | El propio TS ya decidió que es borrado físico, sin Centro de Recuperación — no hay gap de Papelera que diferir. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7b) |
| Migración de stack — Fase 7b: 404 (no 403) para un recordatorio de otro usuario, vía queryset filtrado por dueño | Réplica exacta del TS, que no revela si el recordatorio existe. Distinto de Notas, donde el TS sí usa 403 para no-participantes. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7b) |
| Migración de stack — Fase 7b: `convert-to-task` se difiere aunque `Task` ya existe en Django | Duplica los requisitos de `POST /api/tasks` — feature propia, no una simple FK. Mejor en su propia sub-fase (7c). | `AUDIT_LOG.md` § 2026-08-17 (Fase 7b) |
| Migración de stack — Fase 7b: repetición mensual clampea en vez de replicar el desborde de `Date.setUTCMonth` de JS | Evita sumar `python-dateutil` solo para un edge case (vencimientos día 29-31) sin evidencia de estar cubierto por tests en el TS original. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7b) |
| Migración de stack — Fase 7a: solo Notas (`DeskNote`) en esta sub-fase, Recordatorios queda para 7b | Escritorio Digital tiene 2 sub-dominios separables que solo comparten `DeskAuditLog` — mismo patrón de descomposición incremental ya usado en Proyectos (5a-5f). | `AUDIT_LOG.md` § 2026-08-17 (Fase 7a) |
| Migración de stack — Fase 7a: se reusa `apps.notifications.services.notify()` sin extenderlo para las notificaciones de Escritorio Digital | Ya existe desde la Fase 3f con `task_id`/`task_title` opcionales — no hacía falta generalizar el modelo `Notification` para un 2do consumidor que no necesita esos campos. | `AUDIT_LOG.md` § 2026-08-17 (Fase 7a) |
| Migración de stack — Fase 6c: la pantalla nueva vive en `/reset-password`, sin alternativa de diseño | `send_password_reset_email` ya arma la URL del email como `{FRONTEND_URL}/reset-password?token=...` — la ruta del frontend debe coincidir exactamente. | `AUDIT_LOG.md` § 2026-08-17 (Fase 6c) |
| Migración de stack — Fase 6c: sin auto-login tras confirmar el reset de contraseña | `PasswordResetService.confirm_reset` revoca TODAS las sesiones del usuario (a diferencia de `change_own_password`, que preserva la actual) — no hay sesión que reutilizar. | `AUDIT_LOG.md` § 2026-08-17 (Fase 6c) |
| Migración de stack — Fase 6b: `activityFormat`/`viewPreferences` queda como excepción híbrida — `GET`/`PATCH /api/auth/me` siguen leyendo/escribiendo ese campo en Postgres | Django no tiene ningún campo equivalente y `session.userId` sigue siendo el `cuid` de Postgres — portar una preferencia de UI menor no valía la inversión en esta sub-fase. | `AUDIT_LOG.md` § 2026-08-17 (Fase 6b) |
| Migración de stack — Fase 6b: `MeView` gana un `patch()` en vez de crear una vista/ruta nueva para el auto-servicio de perfil | `PATCH /admin/users/{id}/` exige el permiso administrativo `usuarios.editar` — no sirve para que un usuario edite su propio registro. Se extendió la vista `GET /auth/me/` ya existente. | `AUDIT_LOG.md` § 2026-08-17 (Fase 6b) |
| Migración de stack — Fase 6b: `change-password` adopta sin objeción el comportamiento más rico de Django (revoca otras sesiones + email real) | El legacy de Next.js no hacía ninguna de las dos cosas — se acepta como mejora de seguridad, no como regresión a evitar. | `AUDIT_LOG.md` § 2026-08-17 (Fase 6b) |
| Migración de stack — Fase 6a: modelo híbrido de IDs — Django autentica, `nexo-session` sigue guardando el `cuid` de Postgres vía `legacy_postgres_id` | Cortar el espacio de IDs completo ahora hubiera obligado a migrar los 19 módulos restantes en la misma entrega. Decisión explícita del usuario, elegida sobre el cutover completo de IDs. | `AUDIT_LOG.md` § 2026-08-14 (Fase 6a) |
| Migración de stack — Fase 6a: `src/lib/rate-limit.ts` (Prisma, por IP) se retira del login, no se reconcilia con el rate-limit de Django | Dos sistemas con dimensiones distintas (IP vs `identifier`) — se dejó ganar al de Django, más completo (también bloqueo por `identifier`, timing-safe). | `AUDIT_LOG.md` § 2026-08-14 (Fase 6a) |
| Migración de stack — Fase 6a: `must_change_password`/`lastLoginAt` quedan como gaps aceptados, sin portar | El primero es un concepto nuevo de Django sin superficie en el frontend todavía (no es una regresión); el segundo se pierde porque `AuthenticationService` no pasa por `django.contrib.auth.login()` — dato informativo, no bloqueante. | `AUDIT_LOG.md` § 2026-08-14 (Fase 6a) |
| Migración de stack — Fase 5f: `DELETE /api/projects/[id]` queda en Prisma sin cortar, mezclado con `GET`/`PATCH` (Django) en el mismo `route.ts` | La Papelera de Proyectos sigue leyendo exclusivamente Postgres — cortar el DELETE dejaría un proyecto enviado a la papelera "perdido", sin aparecer nunca ahí. Cada handler HTTP es independiente en Next.js App Router. | `AUDIT_LOG.md` § 2026-08-14 (Fase 5f) |
| Migración de stack — Fase 5f: `roles` en `ProjectUserRefSerializer` y 4 conteos (`phase_count`/`comment_count`/`document_count`/`activity_count`) se cierran en Django antes del adaptador | Son campos obligatorios (no opcionales) en el frontend (`ProjectComment.author.role`, `ProjectListItem._count`) — no resolubles solo del lado Next.js, se cierran en el serializer con el mismo patrón que `participant_count`. | `AUDIT_LOG.md` § 2026-08-14 (Fase 5f) |
| Migración de stack — Fase 5f: el enmascarado de email no vive en `djangoProjectsAdapter.ts` | Depende de la sesión REAL de Next.js (`canManageUsers(session.role)`), no de la de Django — mismo reparto de responsabilidades que tenía Prisma. | `AUDIT_LOG.md` § 2026-08-14 (Fase 5f) |
| Migración de stack — Fase 5e: Actividades de Proyectos no valida solapamiento de horarios, a diferencia de Actividades de Tareas | El TS de Proyectos (`activities/route.ts`) no tiene `findOverlappingActivity` — se replica tal cual, sin inventar una validación que el original no tiene. | `AUDIT_LOG.md` § 2026-08-14 (Fase 5e) |
| Migración de stack — Fase 5e: `businessTime.ts`/`systemConfig.ts`/`timeOverlap.ts` ya estaban portados (vía Tareas 3b/3f), corrigiendo una suposición desactualizada del ROADMAP | Investigar antes de planificar evitó portar infraestructura duplicada — `apps/tasks/business_time.py` se reutiliza tal cual (cross-app import de utilidad pura). | `AUDIT_LOG.md` § 2026-08-14 (Fase 5e) |
| Migración de stack — Fase 5d: `activity_id` (vínculo a `ProjectActivity`) no se porta en `ProjectDocument` | `ProjectActivity` no existe todavía en Django — mismo criterio que 5c (valor neutro documentado, no un comportamiento inventado). | `AUDIT_LOG.md` § 2026-08-13 (Fase 5d) |
| Migración de stack — Fase 5d: `previous_version_id` es un `IntegerField` sin FK | Réplica fiel del schema Prisma original ("referencia suelta, permite historial sin encadenar borrados") — no se "mejora" con una relación real. | `AUDIT_LOG.md` § 2026-08-13 (Fase 5d) |
| Migración de stack — Fase 5c: `registered_minutes`/`participants` de cada fase quedan fijos en `0`/`[]` | `getPhaseStats` agrega `ProjectActivity`, sin portar todavía — mismo valor que el propio TS ya devuelve para una fase recién creada. | `AUDIT_LOG.md` § 2026-08-13 (Fase 5c) |
| Migración de stack — Fase 5b: `comments` (GET y POST) usa el mismo permiso `CanAccessProject` para ambos métodos | `isProjectParticipant` (gate de POST en el TS) y `canViewProject` (gate de GET) son la misma fórmula booleana una vez comparadas — verificado, no una relajación. | `AUDIT_LOG.md` § 2026-08-13 (Fase 5b) |
| Migración de stack — Fase 5b: los 2 `409 Conflict` del TS (participante duplicado / no se puede quitar al responsable) se preservan explícitos | El chequeo vive en la vista (mismo patrón que `CloseMonthView`), no hace falta simplificar a 400 como en el gap ya aceptado de Tareas 3b. | `AUDIT_LOG.md` § 2026-08-13 (Fase 5b) |
| Migración de stack — Fase 5a: Proyectos se construye solo en Django esta sub-fase, sin cortar `route.ts` de Next.js | Django/Postgres son bases separadas — un proyecto creado vía Django rompería sus pestañas de Participantes/Fases/Actividades (todavía en Postgres). Decisión explícita del usuario, confirmada vía pregunta directa. | `AUDIT_LOG.md` § 2026-08-13 (Fase 5a) |
| Migración de stack — Fase 5a: `ROLE_LEVEL` numérico se duplica en `apps/projects/permissions.py`, no se centraliza en `apps.hierarchy` | Mismo criterio ya usado para constantes chicas de un solo consumidor (`ROLE_LABEL` en `apps/tasks/services.py`) — se generaliza cuando aparezca un segundo consumidor real. | `AUDIT_LOG.md` § 2026-08-13 (Fase 5a) |
| Migración de stack — Fase 5a: `Project.deleted_at` es un soft-delete directo, sin Centro de Recuperación (`recoveryCenter.ts`) | Esa pieza es transversal a otros módulos y su propio schema Prisma ya documenta `deletedAt` como "bandera local de conveniencia" — portarla completa está fuera de proporción para un CRUD core. | `AUDIT_LOG.md` § 2026-08-13 (Fase 5a) |
| Migración de stack — Fase 4m: `GET /api/analytics/[userId]` elegido como primer endpoint HTTP de Analytics, sobre `/insights/[userId]` y ~10 endpoints de team/simulación | Es el más chico, con un solo consumidor real (`AdvancedAnalyticsPanel`), y sienta las bases para `/insights/[userId]` (que depende del mismo bundle). | `AUDIT_LOG.md` § 2026-08-12 (Fase 4m) |
| Migración de stack — Fase 4m: import local de `run_analytics_pipeline` dentro de `build_analytics_bundle_payload`, no al tope de `services.py` | Un import a nivel de módulo creaba un ciclo real (`services → pipeline → history → services`) — mismo patrón de import diferido que `normalization.get_effective_curve`. | `AUDIT_LOG.md` § 2026-08-12 (Fase 4m) |
| Migración de stack — Fase 4m: `validation_failures` se renombra a `validation_warnings` del lado Django (`services.py`), no en el adaptador de Next.js | No es un renombre mecánico snake_case→camelCase — mismo patrón ya usado en `build_kpi_payload` (Fase 4b); mantiene `djangoAnalyticsAdapter.ts` 100% genérico. | `AUDIT_LOG.md` § 2026-08-12 (Fase 4m) |
| Migración de stack — Fase 4m: `cache_active` siempre `false`, sin portar la caché en memoria por-proceso del TS | El frontend requiere el campo como no-opcional; portar una caché real es infraestructura nueva fuera de proporción para este endpoint. | `AUDIT_LOG.md` § 2026-08-12 (Fase 4m) |
| Migración de stack — Fase 4l: `_round2` en `validate_analytics_consistency` evita que `round()` lance `OverflowError` con `Infinity` | `Math.round` en JS nunca lanza; sin este helper, el propio chequeo `valor_no_finito` (pensado para detectar `Infinity`/`NaN`) se rompía antes de poder ejecutarse. | `AUDIT_LOG.md` § 2026-08-12 (Fase 4l) |
| Migración de stack — Fase 4l: `compute_prediction` usa `_month_bounds` (UTC) en vez de reimplementar el `monthBounds` local-time del TS | Ese `monthBounds` es un artefacto aislado del TS; todo el resto de este backend es deliberadamente UTC-only — solo afecta un conteo de días redondeado, no una fórmula. | `AUDIT_LOG.md` § 2026-08-12 (Fase 4l) |
| Migración de stack — Fase 4l: el objeto `diagnostics` (contador de cache/validaciones en memoria) no se porta | Instrumentación de proceso Next.js sin consumidor en Django, no una regla de negocio. | `AUDIT_LOG.md` § 2026-08-12 (Fase 4l) |
| Migración de stack — Fase 4l: `run_analytics_pipeline` llama sus 10 dependencias en secuencia, no con `Promise.all` | Django/estas funciones ya son síncronas — mismo criterio que `compute_alerts` (Fase 4i). | `AUDIT_LOG.md` § 2026-08-12 (Fase 4l) |
| Migración de stack — Fase 4k: `prioritizeRecommendations<T>` se porta sin genéricos, como función sobre `list[dict]` | El caso de uso real solo necesita 5 campos por item; Python no tiene un equivalente directo económico y no hace falta la capa de tipado genérico. | `AUDIT_LOG.md` § 2026-08-12 (Fase 4k) |
| Migración de stack — Fase 4k: S6-C/D/G/H se implementan las 4 en `insights_engine.py`, no en archivos separados | Ninguna depende de consumidores externos nuevos y el TS original tampoco las separa — mismo criterio "1 archivo TS = 1 módulo Python" de 4j. | `AUDIT_LOG.md` § 2026-08-12 (Fase 4k) |
| Migración de stack — Fase 4j: `insightsEngine.ts` se divide en 4j (núcleo de Insights) + 4k (relaciones/benchmark/reevaluación/priorización) | Es ~4× más grande que la sub-fase mayor portada hasta ahora; se dividió por cohesión de consumo (lo que el panel de Insights usa junto), no por tamaño arbitrario. | `AUDIT_LOG.md` § 2026-08-12 (Fase 4j) |
| Migración de stack — Fase 4j: `audit_history.py` porta solo `getFactorAuditHistory`/`closestFactorPoint`, no todo `analyticsAuditHistory.ts` | `getScoreSeries` (gráficos) no tiene consumidor todavía — mismo criterio de "se porta cuando haga falta" ya usado en `explain.py` (Fase 4b). | `AUDIT_LOG.md` § 2026-08-12 (Fase 4j) |
| Migración de stack — Fase 4i: `alerts_engine.py` es un módulo separado de `risk_alerts.py`, no una fusión | Verificado leyendo ambos: son 2 motores independientes sin solapamiento de código ni de reglas (4 reglas simples de `/kpis/me` vs. 8 reglas de `analytics.ts`). | `AUDIT_LOG.md` § 2026-08-11 (Fase 4i) |
| Migración de stack — Fase 4i: `get_resolved_alerts_history` porta el manejo de errores sin logging, tal cual el TS | Es la única excepción al resto del motor (que sí loguea best-effort) porque el propio TypeScript tampoco registra el error ahí. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4i) |
| Migración de stack — Fase 4h: tests de `compute_operational_risk` mockean sus 4 dependencias externas en vez de construir datos reales por escenario | Esas 4 funciones ya tienen cobertura propia (4b/4d/4f); mockear aísla la orquestación nueva (8 fórmulas de severidad) sin reconstruir feriados/permisos/16 semanas de historial por cada test. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4h) |
| Migración de stack — Fase 4g: Equilibrio Operativo elegido sobre Riesgo Operativo como siguiente pieza grande | Ambas de tamaño similar, ambas desbloqueadas por Capacidad Proyectada (4f) — el usuario eligió la opción recomendada (2do KPI más consumido). | `AUDIT_LOG.md` § 2026-08-11 (Fase 4g) |
| Migración de stack — Fase 4f: `_build_leave_maps` reimplementa la agrupación multi-usuario en vez de generalizar `get_leave_minutes_by_day` | Esa función es de 1 solo usuario; llamarla en loop habría significado N queries — el legacy batchea en 1 sola consulta, se replica igual. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4f) |
| Migración de stack — Fase 4e: `compute_performance_score` vive en un módulo nuevo (`performance_score.py`), no en `scoring.py`/`history.py` | Necesita funciones de ambos módulos y `history.py` ya importa de `scoring.py` — ponerlo en cualquiera de los 2 habría creado un ciclo de imports. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4e) |
| Migración de stack — Fase 4e: `weighted_points`/`audit_calculation` son helpers genéricos en `scoring.py`, reutilizables por Health Score/Riesgo Operativo | Mismo criterio que `ANALYTICS_CONFIG_DEFAULTS` en 4d — portar la pieza compartida una sola vez en vez de duplicarla en cada sub-fase futura. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4e) |
| Migración de stack — Fase 4d: infraestructura común ("Tanda A") elegida sobre Performance Score directo | Ninguna de las 4 piezas grandes del núcleo (Health/Performance/Riesgo/Alertas) es portable de forma aislada; el usuario eligió la opción recomendada, a diferencia de 4b donde eligió la agresiva. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4d) |
| Migración de stack — Fase 4d: `get_effective_curve` vive en `apps.analytics.normalization`, no en `apps.configuration.services` | Evita invertir la dirección de dependencia entre apps (`analytics` depende de `configuration`, nunca al revés) — corregido antes de escribir tests. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4d) |
| Migración de stack — Fase 4d: `computeWeeklyHistory` replicado tal cual con su límite viernes-00:00-UTC (no fin de día) | Es un límite exacto del legacy, verificado con test explícito — no se "arregla" silenciosamente durante el port. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4d) |
| Migración de stack — Fase 4c: `/api/kpis/me/range` elegido sobre el núcleo de scoring de `analytics.ts` como siguiente sub-fase | Tamaño acotado (267 líneas) y alta reutilización de piezas ya portadas en 4a/4b — el usuario eligió la opción recomendada, a diferencia de 4b donde eligió la agresiva. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4c) |
| Migración de stack — Fase 4c: `monthly_business_base_for_users` expone `start`/`end`, a diferencia de `business_base_for_range` | Ningún consumidor anterior los necesitaba; este caller sí (para `business_day_real_range`) — se agregan sobre una copia local, sin tocar la firma existente. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4c) |
| Migración de stack — Fase 4b: alcance ampliado a 3 funciones puntuales de `analytics.ts` para habilitar el primer endpoint real de KPIs | El usuario eligió explícitamente la opción agresiva sobre la recomendada (`AskUserQuestion`); se tocan solo `computeSimpleScore`/`computeEstimatedVsRealRatio`/`validateCumplimientoConsistency`, no el resto de `analytics.ts`. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4b) |
| Migración de stack — Fase 4b: `GET /api/kpis/me/range` queda fuera de esta sub-fase | Usa la Definición A de "cumplimiento" (`computeCompletedPctAny`), deliberadamente distinta e inconsistente con la Definición B que usa este endpoint — mezclarlas a medio portar generaría confusión. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4b) |
| Migración de stack — Fase 4b: `djangoKpisAdapter.ts` usa transformación recursiva snake_case→camelCase, no mapeo campo por campo | Verificado que el payload (~100 campos hoja) es 100% mecánico sin renombres — la excepción a la convención de mapeo manual está documentada en el propio archivo. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4b) |
| Migración de stack — Fase 4a: KPIs/Analytics se divide en sub-fases por motor/dependencia, no por endpoint | El motor es demasiado grande (`analytics.ts` 2430 líneas) para cortar endpoint por endpoint como Tareas; la base horaria + 3 dependencias de datos nuevas (`LeaveRecord`/`SpecialStatus`/`kpiStartDate`) van primero porque casi todo lo demás las consume. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4a) |
| Migración de stack — Fase 4a: comparación en paralelo Next.js↔Django diferida, se verifica con datos sintéticos como en Tareas | Sin datos reales importados (diferido dos veces), comparar salidas de ambos motores no probaría nada real — confirmado con el usuario antes de planificar. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4a) |
| Migración de stack — Fase 4a: `LeaveRecord`/`SpecialStatus` en `apps.configuration` sin endpoints HTTP, mismo patrón que `Holiday` | Viven bajo `/api/settings/**` (gate ADMINISTRADOR) en el legacy, misma categoría que Holidays — cortarlos repetiría el split-brain Postgres/SQL-Server evitado en 3d. | `AUDIT_LOG.md` § 2026-08-11 (Fase 4a) |
| Migración de stack — Fase 3f: `Notification` sin endpoints HTTP, mismo patrón que `Holiday` (Fase 3d) | `/api/notifications` real sigue en Postgres, alimentado por módulos aún no migrados — cortarlo mostraría una lista incompleta a usuarios reales. | `AUDIT_LOG.md` § 2026-08-11 (Fase 3f) |
| Migración de stack — Fase 3f: lógica de notificación de comentarios/Fecha Fin recuperada vía `git show`, no reconstruida de memoria | Los cutovers de 3a/3c habían borrado el código Prisma original al reescribir esas rutas — se recuperó el mensaje/destinatario exacto de los commits previos. | `AUDIT_LOG.md` § 2026-08-11 (Fase 3f) |
| Migración de stack — Fase 3f: registro retroactivo nunca excluye al propio actor de `retroactiveNotifyRoles` | Verificado en código que el legacy no lo excluye (a diferencia de los comentarios de actividad, que sí) — se porta tal cual, sin "corregir" el legacy. | `AUDIT_LOG.md` § 2026-08-11 (Fase 3f) |
| Migración de stack — Fase 3e: "export de seleccionadas" no requiere trabajo en Django | Es 100% client-side (SheetJS en el navegador, sin round-trip HTTP) y ya opera sobre datos servidos por Django desde la Fase 3a — no hay nada que cortar. | `AUDIT_LOG.md` § 2026-08-07 (Fase 3e) |
| Migración de stack — Fase 3e: `callDjango` ajustado para soportar `FormData` sin forzar `Content-Type: application/json` | El envío de archivos (`multipart/form-data`) necesita que `fetch` genere su propio boundary; cambio compatible hacia atrás, ningún llamador existente pasa `FormData`. | `AUDIT_LOG.md` § 2026-08-07 (Fase 3e) |
| Migración de stack — Fase 3d: alcance ampliado para portar Holidays + Configuración mínima, con endpoints HTTP explícitamente excluidos | El usuario eligió valores reales sobre aproximados para `MonthClosure`, pero cortar `/api/settings/holidays`/`workload-config` reales habría creado split-brain Postgres/SQL Server sobre configuración que Analytics real todavía lee de Postgres. | `AUDIT_LOG.md` § 2026-08-07 (Fase 3d) |
| Migración de stack — Fase 3d: archivado/duplicación de tareas ancla siempre al fin de mes natural, nunca al `cutoff_date` | Verificado en `close-month/route.ts`: `monthEnd` para candidatas usa `nextMonthStartUTC` fijo; el corte solo acota campos informativos/de auditoría. | `AUDIT_LOG.md` § 2026-08-07 (Fase 3d) |
| Migración de stack — Fase 3d: `IsAdministrador` (`is_superuser`) separado de `CanCloseMonth` (`usuarios.editar`) | El legacy usa gates distintos para `/close-month` (3 roles) y `/correct` (solo ADMINISTRADOR) — verificado leyendo ambos archivos completos. | `AUDIT_LOG.md` § 2026-08-07 (Fase 3d) |
| Migración de stack — Fase 3c-bulk: `CanRegularize` es una permission class propia, NO reusa `usuarios.editar` | El legacy usa `CAN_REGULARIZE = [ADMINISTRADOR, JEFE_NACIONAL]` para bulk/pending, deliberadamente más estrecho que `usuarios.editar` (que también incluye COORDINADOR_NACIONAL) — reusarlo habría autorizado de más. | `AUDIT_LOG.md` § 2026-08-07 (Fase 3c-bulk) |
| Migración de stack — Fase 3c-bulk: `getEndDateDataQuality` porta el cálculo real (RECHAZADA cuenta como "no pendiente"), no el comentario del legacy | El código legacy y su propio comentario discrepan; se decidió portar el comportamiento verificado, no la intención documentada — corregir esa discrepancia es una decisión de negocio ajena a esta migración. | `AUDIT_LOG.md` § 2026-08-07 (Fase 3c-bulk) |
| Migración de stack — Fase 3c: autorización de validación reusa `usuarios.editar` en vez de una lista de roles hardcodeada | Verificado en código: es el mismo conjunto de roles que `CAN_VALIDATE_TARGET_TIME_ROLES` legacy en el catálogo sembrado desde la Fase 1 — una sola fuente de verdad. | `AUDIT_LOG.md` § 2026-08-07 (Fase 3c) |
| Migración de stack — Fase 3b: `migrateFijaHistoryIfNeeded` no se porta (no es un gap, no aplica) | Los `Task` de Django nacen todos con `TaskActivity` ya vigente; no hay historial legacy previo que conciliar. | `AUDIT_LOG.md` § 2026-08-07 (Fase 3b) |
| Migración de stack — Fase 3b: Django devuelve 400 uniforme donde el legacy distinguía 400/409 | Verificado que ningún componente del frontend distingue por código de estado antes de aceptar la simplificación. | `AUDIT_LOG.md` § 2026-08-07 (Fase 3b) |
| Migración de stack — Fase 3a: vertical slice de Tareas (ver/crear/editar/eliminar/comentar) cortado ya, el resto en hoja de ruta | 18 rutas es demasiada superficie para una sola pasada; el usuario aceptó que la lista de tareas quede vacía hasta importar datos reales. | `AUDIT_LOG.md` § 2026-08-07 (Fase 3a) |
| Migración de stack — Fase 3a: permiso de ELIMINAR más estrecho que VER/EDITAR/COMENTAR (el asignado no puede borrar) | Replica la asimetría real del Next.js legacy (`DELETE` usaba `canManageUsers`, más estrecho que `canAccessTask`); confirmado con test de regresión tras detectarse como bug real durante el desarrollo. | `AUDIT_LOG.md` § 2026-08-07 (Fase 3a) |
| Migración de stack — Fase 3a: `apps.hierarchy` no conectado a las vistas de Tareas; `currentUserId` de la página sigue siendo el id de Postgres | Ambos son restricciones/gaps MÁS ESTRECHOS que el legacy (nunca de más); preservan una función existente (`view-preferences`) en vez de romperla por una comparación que hoy no tiene efecto real (lista vacía). | `AUDIT_LOG.md` § 2026-08-07 (Fase 3a) |
| Migración de stack — Fase 2: login real sin cambios, login paralelo a Django no bloqueante en cookies separadas | Django todavía no tiene los usuarios reales; el bridge nunca puede afectar el login real si falla o Django está caído. | `AUDIT_LOG.md` § 2026-08-07 (Fase 2) |
| Migración de stack — Fase 2: `/admin/users` se corta a Django sin tocar `UsersManager.tsx`/`admin/users/page.tsx` | Las rutas API de Next.js traducen la forma de los datos; acota el riesgo a la capa de servidor. | `AUDIT_LOG.md` § 2026-08-07 (Fase 2) |
| Migración de stack — Fase 2: password inicial cambia a `"NexoTemporal2026!"`, DELETE pasa a baja lógica, reset de password ya no fija un valor conocido | Los validadores/diseño de seguridad de skelleton_base no se relajan para igualar el comportamiento viejo de Nexo — son mejoras de postura, documentadas como cambio explícito. | `AUDIT_LOG.md` § 2026-08-07 (Fase 2) |
| Migración de stack: estrategia strangler incremental (backend Django + frontend React nuevos en `backend/`/`frontend/`, conviven con Next.js sin tocarlo) en vez de big-bang | App de producción en uso activo (149 rutas, 40 modelos); big-bang es meses sin puntos de rollback. Fase 1 (núcleo de seguridad) no corta tráfico real. | `AUDIT_LOG.md` § 2026-08-07 |
| Migración de stack: jerarquía de roles de Nexo modelada como datos (`apps.hierarchy.RoleVisibility`/`RoleNotificationTarget`) en vez de permisos de catálogo o nivel numérico | `skelleton_base` es deliberadamente plano; las reglas legacy (`roles.ts`) no son consistentes con un solo nivel numérico. Test de regresión parsea `roles.ts` en cada corrida. | `AUDIT_LOG.md` § 2026-08-07 |
| Migración de stack: usuarios legacy se importan con fallback perezoso bcrypt→Argon2, sin resetear contraseñas | Evita golpe de soporte sobre app en uso diario; Django re-encripta automáticamente en el primer login exitoso. | `AUDIT_LOG.md` § 2026-08-07 |
| Migración de stack: fix de causa raíz en migración heredada de `skelleton_base` (`create_permissions` manual antes de leer el catálogo) | Bug real descubierto con pytest-django: Django solo crea `Permission`/`ContentType` en `post_migrate`, emitida una sola vez al final de TODA la invocación de `migrate`, no entre migraciones de la misma invocación. | `AUDIT_LOG.md` § 2026-08-07 |
| Executive Reporting Engine 2.0: rendimiento del mes en curso (~22s) queda como limitación conocida documentada, `predictionEngine.ts`/`analytics.ts` intactos | Causa raíz: 4 funciones de uso individual llamadas por colaborador (36 llamadas para 9 personas) — no afecta exactitud; regeneraciones en caché bajan a ~3.3s. Corrección real (variantes batch) es un Sprint futuro acotado, decisión explícita del usuario de no tocar Analytics ahora. | `AUDIT_LOG.md` § 2026-07-28 (Executive Reporting Engine 2.0 — Decisión 8) |
| Executive Reporting Engine 2.0: Snapshot Integrity Validation (validación activa) queda como mejora futura, no se implementa ahora | Integridad estructural ya cumplida por diseño (Builder canónico único + objeto de snapshot único, mismas funciones que Dashboard/Analytics) — decisión explícita del usuario de no agregar verificación adicional en esta versión. | `AUDIT_LOG.md` § 2026-07-28 (Executive Reporting Engine 2.0 — Decisión 9) |
| Executive Reporting Engine 2.0: `ExecutiveReportSnapshot` es un modelo nuevo, no una extensión de `MonthlyReport` | La inmutabilidad (fila nueva por generación) es incompatible con el `@@unique([month,year,scope])`/`upsert` actual; un modelo nuevo además da Snapshot+Report ID a rango/personalizado, que hoy no persisten. | `AUDIT_LOG.md` § 2026-07-28 (Executive Reporting Engine 2.0) |
| Executive Reporting Engine 2.0: backfill legacy siempre `integrityFlag=PARTIAL` | Ningún `MonthlyReport` histórico registró calidad de dato/versión de motor/NOVA estructurado — no hay forma de reconstruir esos campos 1:1. | `AUDIT_LOG.md` § 2026-07-28 (Executive Reporting Engine 2.0) |
| Executive Reporting Engine 2.0: fecha de corte reconstruye cumplimiento vía `completedAt`, no vía historial de status | NEXO no lleva historial de `status` por tarea — aproximación conservadora documentada, no exacta. | `AUDIT_LOG.md` § 2026-07-28 (Executive Reporting Engine 2.0) |
| Executive Reporting Engine 2.0: NOVA reutiliza `ExecutiveReportContext` en vez de un `groundingContext.ts` propio | Ya cumplía exactamente esa función desde la Fase B — un segundo módulo habría duplicado la misma derivación. | `AUDIT_LOG.md` § 2026-07-28 (Executive Reporting Engine 2.0) |
| Executive Reporting Engine 2.0: escenarios predictivos de equipo diferidos, no implementados | No existe motor de predicción a nivel de equipo — narrar sobre datos inexistentes violaría la regla antialucinación del propio FPS. | `AUDIT_LOG.md` § 2026-07-28 (Executive Reporting Engine 2.0), `ROADMAP.md` |
| Executive Reporting Engine 2.0: pantalla y PDF comparten un solo render a HTML | Evita el patrón de dos sistemas de presentación paralelos que ya existe (duplicado) en el flujo antiguo. | `AUDIT_LOG.md` § 2026-07-28 (Executive Reporting Engine 2.0) |
| Executive Reporting Engine 2.0: integración de Fase E aditiva, sin retirar renderers antiguos todavía | Reescribir ~1600 líneas de UI en uso activo sin poder verificar en navegador amerita su propio checkpoint. | `AUDIT_LOG.md` § 2026-07-28 (Executive Reporting Engine 2.0), `ROADMAP.md` |
| Executive Reporting Engine 2.0: repunte de `MonthlyReports.tsx` reutiliza el render HTML del PDF en pantalla en vez de reescribir 7 componentes React | Evita reintroducir la duplicación de presentación (pantalla vs. PDF) que la Fase E ya había resuelto para el documento; imposible que ambos diverjan si comparten función. | `AUDIT_LOG.md` § 2026-07-28 (Executive Reporting Engine 2.0 — Decisión 10a) |
| Fix `data.meta` ausente en reportes LEGACY_MIGRATION: reconstrucción en el límite de lectura (`GET /[reportId]`), no reparación por escritura en la BD compartida | Cero ventana de escritura no solicitada; la fila ya tiene toda la información en sus columnas propias — la reconstrucción es determinista, no una suposición. | `AUDIT_LOG.md` § 2026-07-28 (Fix — Snapshot Meta) |
| Fix Estado General inconsistente: constructor único `resolveEstadoGeneral` (`estadoGeneral.ts`) en la capa de presentación, no ampliar el Índice Ejecutivo a rango/mes cerrado | El Índice Ejecutivo incluye Capacidad Futura (proyección hacia adelante), no representativa fuera del mes en curso — la regla de negocio ya existente se mantiene intacta; "Sin datos" solo cuando el snapshot está realmente vacío, sin importar el tipo de reporte. | `AUDIT_LOG.md` § 2026-07-28 (Fix — Estado General) |
| Fix Estado General: aproximación de respaldo reutiliza `classifyIndiceEjecutivo` (mismos umbrales/etiquetas), aplicada a Cumplimiento + proximidad de Carga al 100% ideal, en vez de una escala paralela | Evita una segunda tabla de umbrales que alguien tendría que mantener sincronizada con la del Índice Ejecutivo completo; cero Prisma/Analytics nuevo, solo `teamSummary` ya presente en el snapshot. | `AUDIT_LOG.md` § 2026-07-28 (Fix — Estado General) |
| Fix build roto (límite cliente/servidor): `classifyIndiceEjecutivo` se extrae a `executiveReporting/indiceEjecutivo.ts` (sin `"server-only"`), no se duplica ni se suprime el diagnóstico de Turbopack | La función no tiene I/O — solo estaba en un archivo `"server-only"` por colocación histórica; extraerla resuelve la causa raíz (el bundle de cliente ya no incluye código server-only) en vez de esconder el síntoma con `turbopack.ignoreIssue` o duplicar la regla. | `AUDIT_LOG.md` § 2026-07-28 (Fix — build roto, límite cliente/servidor) |
| Fix `data.meta`: `SnapshotMeta` permanece un tipo obligatorio; la defensa vive en runtime solo en el límite Postgres→tipos (`documentModel.ts`), no relajando el tipo globalmente | Evitar `?.`/fallbacks innecesarios en decenas de sitios que hoy pueden confiar legítimamente en que `meta` está completo (todo snapshot `GENERATED`). | `AUDIT_LOG.md` § 2026-07-28 (Fix — Snapshot Meta) |
| Motor de Cierre Inteligente: `monthlyBusinessBase` trunca su `end` de forma transparente (consulta `MonthClosure` internamente) en vez de agregar un parámetro de cutoff a su firma | Evita propagar un parámetro nuevo por ~15 call sites (Analytics/KPIs/Reporting/dashboard); cumple el requisito de compatibilidad total con cierres normales/históricos. | `AUDIT_LOG.md` § 2026-08-02 (Motor de Cierre Inteligente con Fecha de Corte) |
| Motor de Cierre Inteligente: `closureType` NORMAL/EARLY/MANUAL — EARLY si el cierre se ejecuta antes de que el período termine, MANUAL si el período ya terminó y el corte se elige después | Distinción confirmada explícitamente por el usuario — el spec original no la definía. | `AUDIT_LOG.md` § 2026-08-02 (Motor de Cierre Inteligente con Fecha de Corte) |
| Motor de Cierre Inteligente: la Fecha de Corte acota solo la ventana de cálculo (KPIs/Analytics/Reporting), no el archivado de tareas de "Cerrar Mes" | El archivado/duplicación de tareas recurrentes no fue parte del problema planteado; cambiarlo habría sido un cambio de alcance no pedido. | `AUDIT_LOG.md` § 2026-08-02 (Motor de Cierre Inteligente con Fecha de Corte) |
| Motor de Cierre Inteligente: `RANGO_MESES` hereda el cierre del ÚLTIMO mes del rango | Mismo criterio que `resolveMonthlyPeriodStatus`/`resolveRangePeriodStatus`, que ya evaluaban solo el último mes. | `AUDIT_LOG.md` § 2026-08-02 (Motor de Cierre Inteligente con Fecha de Corte) |
| Motor de Cierre Inteligente: sin bump de `ANALYTICS_ENGINE_VERSION`/`FORMULA_SET_VERSION` | Mismo criterio que Base Horaria Efectiva (§15, `ANALYTICS_FORMULAS.md`): se acota el rango de fechas de una fórmula existente, no se modifica la fórmula. | `AUDIT_LOG.md` § 2026-08-02 (Motor de Cierre Inteligente con Fecha de Corte) |
| Validación de Fecha Fin: `canValidateEndDate` reusa `CAN_VALIDATE_TARGET_TIME_ROLES` de `targetTime.ts` en vez de un campo `managerId` nuevo | NEXO no tiene "jefe directo" explícito en `User` — la autorización siempre fue por jerarquía de roles; Tiempo Objetivo ya estableció ese criterio para "quién valida". | `AUDIT_LOG.md` § 2026-08-03 (Validación de Fecha Fin de Subordinados) |
| Validación de Fecha Fin: Rechazar no cambia `Task.endDate`, solo el estado a RECHAZADA | Confirmado con el usuario — la tarea sigue funcionando con la fecha actual; el colaborador debe reproponerla. | `AUDIT_LOG.md` § 2026-08-03 (Validación de Fecha Fin de Subordinados) |
| Validación de Fecha Fin: reeditar `endDate` tras un estado terminal lo reinicia a PENDIENTE de forma transparente (sin UI/endpoint de "solicitar cambio") | Confirmado con el usuario — esa edición ES la nueva solicitud de aprobación; menos superficie nueva que un flujo de solicitud separado. | `AUDIT_LOG.md` § 2026-08-03 (Validación de Fecha Fin de Subordinados) |
| Validación de Fecha Fin: el bulk tool solo APRUEBA en bloque (no fija un valor nuevo como el bulk de Tiempo Objetivo) | Cada tarea tiene su propia fecha correcta — fijar la misma fecha a tareas distintas no tendría sentido. | `AUDIT_LOG.md` § 2026-08-03 (Validación de Fecha Fin de Subordinados) |
| Consolidación de validación: 1 sola tabla/modal/pantalla (Tiempo Objetivo) en vez de pestaña separada para Fecha Fin | Corrección explícita del usuario a la ubicación de v1.25.0 — "reutilizar la arquitectura existente" significaba la misma pantalla, no una paralela. | `AUDIT_LOG.md` § 2026-08-03 (Consolidación de la validación de líder en Tiempo Objetivo) |
| Consolidación de validación: el listado de pendientes pasa a ser una UNIÓN (Tiempo Objetivo O Fecha Fin pendiente), no solo el filtro original de Tiempo Objetivo | Evita que una tarea pendiente únicamente en Fecha Fin quede invisible en la pantalla de gestión. | `AUDIT_LOG.md` § 2026-08-03 (Consolidación de la validación de líder en Tiempo Objetivo) |
| Consolidación de validación: `ValidateActivityModal` hace 2 fetch/submit independientes a los endpoints existentes, no un endpoint POST combinado nuevo | Las 2 validaciones son decisiones independientes con modelos de datos distintos — combinar el submit forzaría una transacción artificial entre 2 conceptos separados a propósito. | `AUDIT_LOG.md` § 2026-08-03 (Consolidación de la validación de líder en Tiempo Objetivo) |
| Aprobación masiva de Fecha Fin: el servidor decide APROBAR vs MODIFICAR comparando `newEndDate` contra el `endDate` vigente, no el cliente | El servidor es la fuente de verdad de "¿esto cambió realmente?" — evita notificaciones/auditoría espurias si el cliente reenvía el mismo valor prellenado. | `AUDIT_LOG.md` § 2026-08-03 (Aprobación masiva con edición) |
| Aprobación masiva de Fecha Fin: `POST /bulk-approve` cambia su contrato de `taskIds` a `items` con fecha opcional, sin mantener compatibilidad con la forma vieja | Único consumidor (el propio modal), sin necesidad de una ruta de compatibilidad transitoria. | `AUDIT_LOG.md` § 2026-08-03 (Aprobación masiva con edición) |
| Executive Reporting Engine 2.0: 3 campos del snapshot sin página fija (tendencias, evolución de rango, alertas) se agregan como paneles complementarios en pantalla | El documento de 11 páginas del FPS no los incluye, pero SÍ existen en el snapshot ya calculado; omitirlos habría sido una regresión funcional real (alertas de gestión activamente usadas por dirección), no cosmética. | `AUDIT_LOG.md` § 2026-07-28 (Executive Reporting Engine 2.0 — Decisión 10b) |
| Executive Reporting Engine 2.0: el asistente ("Generador Inteligente") redefine "secciones" como las 9 páginas reales del documento, no las 10 claves ad-hoc de `wizardExport.ts` | Evita mantener una segunda implementación de armado de HTML/Excel en paralelo a los renderers compartidos; efecto secundario aceptado: ya no hay una sección "Tendencias" exportable por separado (esa página no existe en el modelo de 11 páginas). | `AUDIT_LOG.md` § 2026-07-28 (Executive Reporting Engine 2.0 — Decisión 10c) |
| Sprint O: "shell ahora, greenfield después" — Centro de Configuración completo + 9 valores de bajo riesgo, resto como "Próximamente" | Proyectos-SLA/Reportes-plantillas/Seguridad-permisos especiales son subsistemas nuevos desde cero (scheduler+email, motor de plantillas, nueva capa de autorización), no config de algo existente — el propio pedido ya los nombraba "Sprint K"/"Sprint F" aparte. | `AUDIT_LOG.md` § 2026-07-28 (Sprint O) |
| Sprint O: Parámetros Globales queda de solo lectura (zona horaria/primer día de semana/formato de fecha) | Se consumen de forma síncrona en todo el núcleo de Analytics (`analytics.ts`/`workload.ts`/`trendEngine.ts`/`insightsEngine.ts`/`capacityForecast.ts`) — mismo riesgo arquitectónico que "días laborables", ya diferido por el usuario. | `AUDIT_LOG.md` § 2026-07-28 (Sprint O) |
| Sprint O: fix de role-gate corrige `page.tsx`, no amplía `SettingsManager.tsx` | 2 de 3 fuentes (link de navegación + gate interno del componente) ya coincidían en Administrador-only; Coordinador Nacional nunca tuvo acceso real al contenido, solo pasaba el gate de ruta. | `AUDIT_LOG.md` § 2026-07-28 (Sprint O) |
| Fix "Carga Laboral": el indicador ahora lee `cargaTiempo.mensual` en vez de reimplementar un cálculo propio | Garantiza igualdad byte-a-byte con WorkloadCard (misma pantalla), que ya calcula y envía ese objeto — cero coste adicional, cero fórmula nueva. | `AUDIT_LOG.md` § 2026-07-28 |
| Fix "Carga Laboral": `WorkloadColor` (orange incluido) colapsa a `KpiColor` con `orange→yellow` | Evita subestimar (orange→green) o sobreestimar (orange→red, igualándolo a Sobrecarga real) una carga ya elevada pero no crítica. | `AUDIT_LOG.md` § 2026-07-28 |
| Compatibilidad Organizacional: Regla 4 (nunca redistribución vertical) es un filtro absoluto en `computeTeamRecommendations`, no solo validación de configuración | El pedido pide que se "descarten automáticamente" — invariante del motor, no un checkbox de formulario que se pueda saltear editando la BD directamente. | `AUDIT_LOG.md` § 2026-07-26 (Compatibilidad Organizacional) |
| Compatibilidad Organizacional: Matriz de Compatibilidad Operativa es direccional, no auto-simétrica | Mismo patrón de almacenamiento que `getEffectiveRoleTarget` (un valor JSON por cargo); más simple de auditar a costa de configurar ambos lados para compatibilidad mutua. | `AUDIT_LOG.md` § 2026-07-26 (Compatibilidad Organizacional) |
| Sprint E: motor predictivo como módulo nuevo y autónomo (`/inteligencia-preventiva`), no integrado aún en Dashboard/Analytics/Reportes/Proyectos/Equipo | El pedido pide explícitamente no modificar esas pantallas todavía; la integración profunda queda para un sprint futuro. | `AUDIT_LOG.md` § 2026-07-26 (Sprint E) |
| Sprint E: Capacidad Disponible histórica NO se reconstruye "retrocediendo `now`" — usa el factor de auditoría de Equilibrio Operativo | `Task.status` es mutable y sin tabla de historial; backdatear `now` mezclaría matemática de negocio del pasado con asignaciones de tareas de hoy — bug real detectado antes de implementar. | `AUDIT_LOG.md` § 2026-07-26 (Sprint E), `ANALYTICS_FORMULAS.md` §16 |
| Sprint E: simulador de 3 escenarios nuevos vive en rutas separadas, no extiende `/api/analytics/simulate/[userId]` | 2 de los 3 escenarios nuevos son bi-usuario/proyecto y cambiarían el contrato de esa ruta para sus consumidores existentes, protegidos ese sprint. | `AUDIT_LOG.md` § 2026-07-26 (Sprint E) |
| Sprint E: "Consultas" (consultas a Nova) fuera de alcance del Trend Engine | No existe tabla que registre preguntas a Nova; agregar ese logging es una pieza de producto/privacidad aparte, no una decisión unilateral dentro del sprint. | `AUDIT_LOG.md` § 2026-07-26 (Sprint E), `ROADMAP.md` |
| Excepción de fin de semana en registro retroactivo: solo Seguimiento/Proyectos, no Fija | Fija nunca tuvo registro retroactivo (decisión previa de 2026-07-21); extenderle una excepción sobre una capacidad que no existe habría sido una expansión de alcance no pedida. | `AUDIT_LOG.md` § 2026-07-26 |
| Cambio de "Horas Estimadas" → "Tiempo Objetivo" | Separar la estimación subjetiva del colaborador de un estándar oficial que un líder puede validar, sin migrar la columna física de Prisma. | `AUDIT_LOG.md` § 2026-07-21 |
| Unificación de registro Fija/Seguimiento vía `TaskActivity` | Las tareas Fijas no tenían historial ni auditoría de horas; reutilizar el modelo de Seguimiento evita mantener dos sistemas de registro con reglas distintas. | `AUDIT_LOG.md` § 2026-07-21 |
| Migración de historial de Fijas: perezosa, no un script masivo | La base de datos compartida es producción — una migración bajo demanda evita una escritura global irreversible sin necesidad real de serlo. | `AUDIT_LOG.md` § 2026-07-21 |
| Separación de Performance Score y Operational Risk Score | Un único número mezclaba desempeño y riesgo/capacidad, ocultando cuál de los dos requiere intervención. | `AUDIT_LOG.md` § 2026-07-20 |
| Índice de Riesgo Operativo congelado (Sprint 5 §S5-C) | Estabilidad de un indicador usado en reportes de dirección — cambiar sus pesos/reglas sin una decisión explícita rompería comparabilidad histórica. | `docs/ANALYTICS_FORMULAS.md` |
| Cero IA para cálculos de negocio (Decision Intelligence Engine) | Auditabilidad y disponibilidad — un KPI/alerta no puede depender de una llamada no determinista a un LLM ni de que `GROQ_API_KEY` esté configurada. | `AUDIT_LOG.md` § 2026-07-20 |
| Motor de Benchmarks Inteligente — 3 niveles (cargo/limitado/personal) | Muchos cargos de esta organización son de una sola persona; comparar contra el propio historial evita el mensaje "sin datos" sin inventar comparaciones inválidas. | `AUDIT_LOG.md` § 2026-07-20 |
| Modelo de Analytics diferenciado para roles de dirección | Jefe Nacional/Administrador dirigen equipos, no ejecutan tareas — sus KPIs individuales de ejecución no son representativos y distorsionan promedios/recomendaciones del equipo. | `AUDIT_LOG.md` § 2026-07-21 |
| Invalidación de caché de Analytics: global → granular por usuario | La invalidación global era correcta pero recalculaba Analytics de todo el sistema ante el cambio de un solo usuario, una vez que el patrón de acceso maduró lo suficiente para segmentar por clave. | `AUDIT_LOG.md` § 2026-07-21 |
| NormalizationEngine (curvas configurables por indicador) | Distintos indicadores necesitan distintas curvas de sensibilidad (lineal, por tramos) sin hardcodear umbrales — se centralizó en `getEffectiveCurve()`/`DEFAULT_CURVES`, editable desde Ajustes. | `docs/ANALYTICS_FORMULAS.md` |
| `getOfficialTargetTime()` como único accesor de Tiempo Objetivo | Evitar que cada caller reimplemente el fallback `targetTimeValidated ?? estimatedHours` — encontrado repetido antes de consolidarse. | `AUDIT_LOG.md` § 2026-07-21 |
| Prisma 7 con `@prisma/adapter-pg` (driver adapter obligatorio) | `new PrismaClient()` sin adapter lanza error en runtime desde Prisma 7 — se estandarizó la inicialización en `src/lib/prisma.ts` como singleton. | `CLAUDE.md` |
| JWT firmado (`jose`) + cookie httpOnly en vez de sesiones server-side | Sin estado de sesión que persistir en base de datos; el payload (`userId`/`role`/`name`/`email`) es suficiente para las verificaciones de `proxy.ts` sin una consulta adicional en cada request. | `docs/ARCHITECTURE.md` |
| Sistema de diseño v1 (2026-07-03) → v2 (2026-07-05) | La primera versión (modo claro/oscuro) se reemplazó por un rediseño premium con sidebar, tokens de diseño e iconografía nueva, tras validar la dirección visual con el producto. | — |
| Framework de pruebas: Vitest + Testing Library (no Jest) | Integración nativa con Vite/Next 16 sin configuración adicional de transformadores; `vitest.config.ts` ya replica el setup recomendado por la documentación de Next 16 para este propósito. | `CLAUDE.md` |
| Changelog automático vía hook `post-commit` (no `prepare-commit-msg`) | Un `git add` hecho en `prepare-commit-msg` no queda incluido en el commit que se está creando (comprobado empíricamente); enmendar desde `post-commit` sí funciona. | `.githooks/update-changelog.js` |
| Sistema de documentación en `/docs` leído en vivo, sin base de datos nueva | Los Markdown ya viven en el repositorio versionado por Git — una tabla nueva solo para mostrarlos duplicaría una fuente de verdad que ya existe. | Este sprint (2026-07-22) |
| Módulo Proyectos como dominio independiente de Task (no una extensión) | "No cierra por mes" y "sin registro colectivo único" contradicen invariantes ya asumidos por `Task`/`archivedMonth`; un dominio nuevo evita ramas condicionales en código existente. | `AUDIT_LOG.md` § 2026-07-23 |
| Centro de Recuperación: registro de adaptadores (`entityType` como `String`), no un enum de Prisma | Un enum exigiría una migración de schema por cada módulo nuevo dado de alta; un registro en código logra costo-cero real de integración. | `AUDIT_LOG.md` § 2026-07-23 |
| Eliminación de Proyectos acotada solo al creador (no responsable/liderazgo) | El pedido (Sprint 2.1 §3) fue literal — sin excepción de liderazgo, se implementó tal cual en vez de asumir un atajo administrativo no solicitado. | `AUDIT_LOG.md` § 2026-07-23 |
| "Participante" de un proyecto: por asignación explícita o por registrar actividad (nunca automático al crear) | Responsable y participante son conceptos distintos (Sprint 2.1 §2); auto-alta al registrar evita que la primera actividad de alguien quede huérfana de la pestaña Participantes. | `AUDIT_LOG.md` § 2026-07-23 |
| Escritorio Digital excluye al Administrador (crear/recibir/leer/fijar/archivar) | No es un participante operativo del día a día — el pedido lo excluyó explícitamente, sin excepción. | `src/lib/roles.ts` (`canUseDeskNotes`) |
| Escritorio Digital sin jerarquía de destinatarios (a diferencia de `VISIBLE_ROLES`) | Simula un escritorio físico compartido entre colegas, no un canal de reporte ascendente/descendente — cualquier no-Administrador puede dejarle una nota a cualquier otro no-Administrador. | `src/app/api/desk-notes/recipients/route.ts` |
| Escritorio Digital: segundo módulo integrado al Centro de Recuperación, sin pantalla de papelera dedicada todavía | El código ya anticipaba el módulo por nombre en `ENTITY_REGISTRY`; integrar el adaptador cuesta ~10 líneas y respeta "ningún módulo construye su propia papelera", sin inflar el sprint con UI no pedida. | `AUDIT_LOG.md` § 2026-07-23 |
| `FollowUpReminder` migrado y eliminado (no coexistencia temporal) | El pedido dice "eliminar completamente"; mantener ambos sistemas vivos duplica superficie de mantenimiento sin necesidad real. Datos migrados a `PersonalReminder`, verificados antes de `DROP TABLE`. | `AUDIT_LOG.md` § 2026-07-23 |
| Recordatorios: notificación perezosa (`Notification` reutilizado) reemplaza el popup `ReminderNotifier` | El pedido exige reutilizar el sistema de notificaciones existente (§15); un popup flotante persistente contradice el espíritu de un "centro personal" calmado. | `AUDIT_LOG.md` § 2026-07-23 |
| Adjunto de la nota NO se copia al convertir en tarea (solo se referencia por nombre en la descripción) | `Task` no tiene campo de adjunto y el pedido prohíbe modificar Trabajo salvo la eliminación de recordatorios (§15); la nota original con el adjunto real permanece intacta y accesible. | `AUDIT_LOG.md` § 2026-07-23 |
| `PersonalReminder` no se integra al Centro de Recuperación (borrado físico + `DeskAuditLog`) | Ítem de productividad personal de alta rotación — someterlo a retención/restauración es sobre-ingeniería no pedida, a diferencia de `DeskNote` (comunicación entre personas). | `AUDIT_LOG.md` § 2026-07-23 |
| "Proyectos con actividad reciente" (Bandeja Hoy) usa ventana fija de 7 días, no la última visita real | `User` no guarda timestamp de última visita al Escritorio; agregarlo quedaba fuera de alcance. Documentado para no prometer una medición que no existe. | `AUDIT_LOG.md` § 2026-07-23 |
| Reabrir un recordatorio actualiza la fila existente in-place (nunca crea una nueva) | El pedido es literal: "no se creará un nuevo registro", "se conservará el mismo identificador" — preserva `id` y todo el `DeskAuditLog` acumulado. | `AUDIT_LOG.md` § 2026-07-23 |
| Sprint B ejecutado por fases (7 commits) en vez de un único commit masivo | El diff toca ~90 archivos únicos; separar por concern (primitivos/botones/chips/tablas/toasts/modales/docs) permite revertir una fase puntual sin perder las demás si algo falla. | `docs/DESIGN_SYSTEM.md` § Informe de Design Review |
| `PriorityChip`/`StatusChip` como un único componente (no dos) | El propio spec del sprint pide "exactamente el mismo componente visual" para prioridades y para estados — ambos casos son un `value` + un tono, sin diferencia estructural real. | `docs/DESIGN_SYSTEM.md` § 5 |
| Sprint B no toca Ideas/Reuniones/Proyectos completo/Repositorio/Nova/Login/Perfil | Alcance acordado explícitamente: fundación + módulos de alto tráfico completos, resto documentado como backlog priorizado (spec §25 ya anticipa un informe de lo no corregido). | `docs/DESIGN_SYSTEM.md` § Informe de Design Review |
| No se relaja `targetTimeHours` (obligatorio) en Crear Proyecto | Es un campo `Float` no-nullable en Prisma — volverlo opcional exige una migración de schema, explícitamente vetada en un sprint de refinamiento de UX. Se agregó explicación (`InfoTooltip`) en vez de cambiar la validación. | `docs/PRODUCT_REVIEW.md` §7 |
| Sin buscador global unificado (tareas+proyectos+notas) en Sprint C | Requiere una superficie de API nueva — es una iniciativa de producto propia, no una unificación de interacción/UI como el resto del sprint. | `docs/PRODUCT_REVIEW.md` §10 |
| "Acciones inteligentes" (§10 del sprint) acotado a una sola instancia (búsquedas recientes) | Evitar que un refinamiento se convierta en una feature de alcance abierto — proyectos recientes/personas frecuentes quedan documentados como extensión futura del mismo patrón, no descartados. | `docs/PRODUCT_REVIEW.md` §10 |
| `RecoveryError` (clase nueva en `recoveryCenter.ts`) para el fallback de error de las rutas de Papelera | `err instanceof Error` es verdadero también para excepciones inesperadas (ej. Prisma) — sin una clase propia, un error no curado podía filtrarse crudo al usuario. | `docs/PRODUCT_REVIEW.md` §4 |
| `InfoTooltip` como componente separado de `HelpPopover` (Analytics) | `HelpPopover` está acoplado a `IndicatorHelp` (4 secciones estructuradas) — forzar esa estructura a una explicación de una línea fuera de Analytics sería sobre-ingeniería. | `docs/DESIGN_SYSTEM.md` |
| Reabrir con nueva fecha audita `REOPENED` + `POSTPONED` (dos eventos, no uno nuevo) | El pedido ejemplifica el historial como dos líneas separadas; "reabrir con nueva fecha" y "posponer" son el mismo cambio de campo, no ameritan un tercer valor de enum. | `AUDIT_LOG.md` § 2026-07-23 |
| `isCompletedOnTime` compara por día calendario (huso de negocio), no por instante UTC crudo | Comparación anterior clasificaba como tardía cualquier tarea cerrada durante el horario laboral real de su propio día de vencimiento (medianoche UTC ≈ 7pm del día anterior en UTC-5) — confirmado con auditoría empírica: 51% de las tareas "fuera de tiempo" eran del mismo día calendario. | `AUDIT_LOG.md` § 2026-07-24 |
| `utcCalendarDay` exportado desde `utils.ts` (antes privado) | `isCompletedOnTime` necesitaba la misma lógica de "día calendario" que ya usaba `isTaskOverdue` — exportarla en vez de reimplementarla evita una 2ª copia de la misma fórmula. | `AUDIT_LOG.md` § 2026-07-24 |
| Backfill de `completedAt` = `endDate` para 33 tareas históricas (migración de una sola vez) | No existe registro del instante real de finalización — fue la causa del problema. `endDate` es la única aproximación disponible que no inventa un dato inexistente; se descartó una tercera categoría "sin dato" en Cumplimiento por ser un cambio de fórmula, fuera de alcance. | `AUDIT_LOG.md` § 2026-07-24 |
| `POST /api/tasks` ahora fija `completedAt` si el estado inicial es Completada | Crear una tarea directamente en estado Completada reproducía hacia adelante el mismo gap que motivó el backfill histórico — los otros dos caminos (editar, corregir) ya lo hacían bien. | `AUDIT_LOG.md` § 2026-07-24 |
| Reabrir un recordatorio archivado lo desarchiva automáticamente (incondicional) | Un recordatorio "archivado pero pendiente" es un estado sin caso de uso pedido; reabrir debe devolverlo a activo en todo sentido. | `AUDIT_LOG.md` § 2026-07-23 |
| "Convertir en Tarea" se retira de las notas — ahora Nota→Recordatorio→Tarea | El pedido reestructura el flujo en dos pasos; se confirmó 0 uso real de la conversión directa antes de eliminar la columna. | `AUDIT_LOG.md` § 2026-07-23 |
| El adjunto se copia (no se referencia) en cada hop de conversión Nota→Recordatorio | La nota original puede purgarse a los 15 días de archivada; una referencia suelta perdería el archivo antes de llegar a Tarea. | `AUDIT_LOG.md` § 2026-07-23 |
| Eliminación definitiva desde Archivadas es un borrado directo, no una segunda papelera | La nota archivada ya tiene su propio reloj de 15 días (§8); envolverla en el Centro de Recuperación además sería una retención duplicada sobre la misma acción. | `AUDIT_LOG.md` § 2026-07-23 |
| Respuestas de nota acotadas a 2, entre remitente y destinatario únicamente | El pedido es explícito: no es un chat. Al límite, la API devuelve el mensaje exacto pedido en vez de una genérica. | `src/app/api/desk-notes/[id]/replies/route.ts` |
| Buscador de Escritorio Digital como overlay, no como pestaña | El pedido exige que buscar no obligue a cambiar de sección; una pestaña "Buscar" contradice eso literalmente. | `src/components/desk/GlobalSearchOverlay.tsx` |
| Analytics Explicativo funciona como una capa superior de interpretación y no modifica el motor oficial de cálculo | Regla de arquitectura explícita del pedido (Sprint A): `analytics.ts`/`capacityForecast.ts`/`workload.ts`/`targetTime.ts`/`normalizationEngine.ts` deben mantenerse como fuente oficial de cálculo. | `docs/ANALYTICS_FORMULAS.md` §14 |
| Historial de auditoría para la capa explicativa vive en `analyticsAuditHistory.ts` (nuevo), no se ampliaron las funciones de lectura de `analytics.ts` | La única alternativa sin este archivo nuevo exigía ampliar la firma de `getScoredAuditHistory()` dentro del motor — se prefirió un adaptador nuevo por la misma regla de no tocar los 5 archivos protegidos. | — |
| Simulador "¿qué pasaría si...?" personal como componente nuevo (`WhatIfSimulator.tsx`), sin tocar `TeamWorkloadCards.tsx` | El simulador de equipo ya está en producción; extender el endpoint (`simulate/[userId]`) con escenarios nuevos y crear un componente aparte evita arriesgar ese flujo existente. | `docs/CHANGELOG.md` § v1.12.0 |
| Sprint D — alcance "solo lo seguro" (bugs/seguridad/deuda técnica/performance, no comportamiento de negocio) | El sprint exige aprobación explícita para cambiar reglas de negocio; de ~40 hallazgos de auditoría, ~8 sí cambiaban comportamiento — se presentaron al usuario en vez de decidir unilateralmente cuáles implementar. | `AUDIT_LOG.md` § Sprint D |
| `formatDuration` estandarizado al formato que omite unidades en cero ("30min"/"6h") | 3 de las 4 copias duplicadas (todas del lado Proyectos) ya usaban ese formato; solo la de Tareas mostraba siempre ambas unidades ("0h 30min") — se migró la minoría a la mayoría en vez de al revés. | `AUDIT_LOG.md` § Sprint D |
| Panel de Calidad del Dato (Sprint D §9) vive dentro de Ajustes, no como módulo nuevo | El sprint prohíbe explícitamente crear módulos nuevos; es un diagnóstico de solo lectura bajo demanda, mismo patrón que el ya existente "Diagnóstico del Motor de Analytics". | `src/components/settings/DataQualitySection.tsx` |
| Se difiere el batching de `computeTeamOperationalRisk`/`computeTeamPerformanceScore` (Sprint D §2) | No es una emergencia de performance hoy (mitigado por `cached()`); refactorizar el interior de funciones de cálculo frágiles para agruparlas implica riesgo real de introducir un bug, fuera del apetito de riesgo de "solo lo seguro". | `AUDIT_LOG.md` § Sprint D |
| Hallazgo de auditoría sobre `PersonalReminder` sin papelera reclasificado de INCONSISTENCIA a INTENCIONAL | Ya era una decisión deliberada de un sprint anterior (fila más arriba, 2026-07-23) que el agente de auditoría no había cruzado contra `DECISIONS.md` antes de reportarlo. | `AUDIT_LOG.md` § Sprint D |
| `docs/PROJECT_HISTORY.md` (mencionado en el spec de Sprint D §10) no se crea | No forma parte del set oficial de documentación de `CLAUDE.md`; es casi seguro una confusión con el modelo `ProjectHistory` de Prisma (historial por proyecto), ya implementado desde v1.5.0. | `CLAUDE.md` § Documentación |
| Sprint D (continuación) — Bloque 7 UX se ejecuta con auditoría dedicada, no se reutiliza el "sin hallazgos" de la primera pasada | La primera pasada acotó UX por falta de hallazgos concretos; el pedido ampliado lo pide explícitamente con categorías propias — un audit de solo lectura nuevo encontró 10 hallazgos reales de markup puro. | `AUDIT_LOG.md` § Sprint D (continuación) |
| Motivo huérfano y retroactivo inconsistente: chequeos reales, no defensivos | A diferencia de "sin propietario"/"huérfanos" (protegidos por FK, siempre 0), `TaskActivity.reason` es un String libre sin FK — es el único punto donde un chequeo nuevo de Calidad del Dato podía encontrar datos reales, no solo confirmar una garantía estructural ya existente. | `src/app/api/settings/data-quality/route.ts` |
| Ventana de 90 días para los chequeos de actividades del panel de Calidad del Dato | Mismo valor ya usado por el chequeo de solapamiento; un click bajo demanda de Administrador no debe escanear el historial completo de actividades (a diferencia de Tarea/Proyecto/Fase, de volumen mucho menor). | `src/app/api/settings/data-quality/route.ts` |
| `Button`/`EmptyState`/`Spinner` migrados en Ideas/Reuniones/Proyectos sin cambiar ningún `onClick`/lógica de validación | El objetivo es visual/markup — se preservó cada handler y cada condición de `disabled` exactamente igual, solo cambió el componente que envuelve el markup. | `AUDIT_LOG.md` § Sprint D (continuación) |
| Rename "Score de Salud Laboral" → "Equilibrio Operativo": solo marca visible + prosa de docs, no símbolos de código ni `AnalyticsAuditLog.kind` | `kind: "health_score"` está persistido en miles de filas históricas y prohibido tocar por las restricciones del sprint ("no tocar historial/auditoría"); renombrar símbolos TS es refactor mecánico sin beneficio funcional. | `AUDIT_LOG.md` § Sprint Analytics 2.0 |
| `capacityToScore` progresivo se activa por `estado === "sobrecarga"`, no por `disponiblePct < 0` | Una sobrecarga leve puede redondear `disponiblePct` a 0 exacto (`disponible` en horas sigue siendo negativo); condicionar por el signo del porcentaje redondeado reintroducía el mismo salto abrupto que el Bloque 9 buscaba eliminar, justo en ese borde. | `AUDIT_LOG.md` § Sprint Analytics 2.0 |
| `NormalizationEngine` no se usa para la curva progresiva de Capacidad Futura | Su curva `capacidad` ya configurable en Ajustes es código muerto y mal calibrada para los anclajes de este sprint; migrarla íntegra habría tocado también el lado positivo, fuera del único cambio matemático autorizado. Queda en `docs/ROADMAP.md`. | `AUDIT_LOG.md` § Sprint Analytics 2.0 |
| `classifyEstadoOperativo` (5 niveles) es una capa de presentación adicional, no reemplaza la clasificación inline de 4 niveles de `HealthScoreResult` | `WhatIfSimulator`/`TeamWorkloadCards`/nova-insights ya hacen pattern-matching sobre `classification`/`classificationColor` — cambiarlos habría sido un refactor no pedido por el sprint. | `docs/ANALYTICS_FORMULAS.md` §3 |
| Bloque 13 ("estándar de explicabilidad para todos los KPIs") acotado a Equilibrio Operativo esta pasada | Confirmado con el usuario vía pregunta explícita; sus 5 dimensiones ya cubren Cumplimiento/Carga/Consistencia/Capacidad. Extender a tarjetas standalone queda en `docs/ROADMAP.md` con el patrón ya construido como plantilla. | `AUDIT_LOG.md` § Sprint Analytics 2.0 |
| El "Análisis IA" (Groq) del Informe Consolidado se mantiene intacto junto a las nuevas secciones deterministas | El Bloque 3 exige reglas para las recomendaciones *nuevas* que pide, no la eliminación de una funcionalidad existente que nadie señaló como problema. | `AUDIT_LOG.md` § Sprint Reportes Ejecutivos 2.0 |
| Índice Ejecutivo del Equipo: promedio de Performance Score + Equilibrio Operativo, no `computeSimpleScore` | El Bloque 11 pide que el índice resuma cumplimiento/productividad/salud operativa/carga/consistencia — `computeSimpleScore` no cubre carga, salud operativa ni consistencia. | `AUDIT_LOG.md` § Sprint Reportes Ejecutivos 2.0 |
| Índice Ejecutivo del Equipo disponible solo para el mes calendario en curso | Equilibrio Operativo incluye Capacidad Futura, una proyección hacia adelante desde "ahora" — no representativa si se recalcula para un mes pasado, y el generador de informes permite regenerar cualquier mes. | `AUDIT_LOG.md` § Sprint Reportes Ejecutivos 2.0 |
| Tendencias mes/trimestre/semestre (Bloque 9) usan `computeSimpleScore`, no el motor completo | Seguro para cualquier mes pasado (sin Capacidad Futura); evita 6+ llamadas extra al motor por miembro solo para 3 tarjetas comparativas. | `AUDIT_LOG.md` § Sprint Reportes Ejecutivos 2.0 |
| Índice Ejecutivo y tendencias mes/trimestre/semestre no se agregan al informe de Rango | Un rango de N meses no tiene un "mes en curso" al cual gatear el índice; el rango ya expone su propia evolución mes a mes, que cubre el mismo propósito. | `AUDIT_LOG.md` § Sprint Reportes Ejecutivos 2.0 |
| `computeTeamMonthlySnapshots` no reemplaza la lógica ya existente en `kpis/executive/route.ts` | Refactorizar una ruta ya estable como efecto colateral de este sprint introduce riesgo no solicitado; la unificación queda en `docs/ROADMAP.md`. | `AUDIT_LOG.md` § Sprint Reportes Ejecutivos 2.0 |
| Base Horaria Efectiva en rangos multi-mes usa la tarifa vigente al INICIO del rango, no mes a mes | Recorrer mes a mes duplicaría la lógica que `range/route.ts` ya tiene para otro propósito (base compartida del equipo); el caso que cambia de tarifa a mitad de un rango prorrateado es un edge case raro, documentado en vez de resuelto con complejidad adicional. | `AUDIT_LOG.md` § Sprint Analytics 2.1 |
| Estado Operativo/Principal Hallazgo para períodos que no son el mes en curso: aproximación derivada, no el motor real | Mismo criterio que el Índice Ejecutivo (Capacidad Futura no es representativa de un período cerrado) — evita invocar `computeHealthScore` con una fecha `now` histórica, un uso del motor nunca validado para ese caso. | `AUDIT_LOG.md` § Sprint Analytics 2.1 |
| PDF Ejecutivo del Generador Inteligente ignora la selección de secciones fuera de un set fijo | "Ejecutivo" implica una versión condensada y predecible para dirección — dejar que cualquier combinación de checkboxes lo alterara contradice el propósito de tener dos variantes de PDF distintas. | `AUDIT_LOG.md` § Sprint Analytics 2.1 |
| Wizard nunca persiste como `MonthlyReport` cuando se pide un subconjunto de colaboradores | El modelo `MonthlyReport` asume siempre el equipo completo (única clave `[month, year, scope]`); persistir un subconjunto rompería ese invariante o exigiría una migración de schema no justificada para un caso de uso ad-hoc. | `src/app/api/reports/generate/route.ts` |
| Comparación de Equipos: solo tipos y stub, sin campo de schema nuevo | NEXO no tiene hoy una dimensión de área/equipo/zona en `User` (solo `role`); inventar un campo sin una decisión de producto explícita sobre qué dimensión usar sería adivinar. | `src/lib/teamComparison.ts` |

---

## Cómo agregar una decisión nueva

Cuando una implementación futura tome una decisión de arquitectura o diseño
técnico no trivial (una alternativa fue descartada por una razón concreta),
agregar una fila a la tabla de arriba. Si la decisión es lo bastante
significativa como para merecer un análisis completo de alternativas
evaluadas, agregar también una entrada en `docs/AUDIT_LOG.md` y enlazarla
desde la columna "Detalle completo".

_Última actualización: 2026-07-24 (Sprint Analytics 2.1)._
